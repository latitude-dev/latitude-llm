name: Build and Push Images

on:
  workflow_call:
    inputs:
      environment:
        description: "Target environment (staging or production)"
        required: true
        type: string
      ref:
        description: "Git ref to build from"
        required: true
        type: string
      aws-region:
        description: "AWS region"
        required: false
        type: string
        default: "eu-central-1"
    outputs:
      image_tag:
        description: "Image tag that was pushed (sha-<commit>)"
        value: ${{ jobs.metadata.outputs.image_tag }}

permissions:
  contents: read
  packages: write

jobs:
  metadata:
    name: Compute image metadata
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    outputs:
      image_tag: ${{ steps.meta.outputs.image_tag }}
      registry: ${{ steps.meta.outputs.registry }}

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ inputs.ref }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 25

      - name: Compute image metadata
        id: meta
        run: |
          SHA=$(git rev-parse --short=8 HEAD)
          OWNER=$(printf '%s' "$GITHUB_REPOSITORY_OWNER" | tr '[:upper:]' '[:lower:]')
          echo "image_tag=sha-${SHA}" >> "$GITHUB_OUTPUT"
          echo "registry=ghcr.io/${OWNER}" >> "$GITHUB_OUTPUT"

  build:
    name: Build and push ${{ matrix.service }}
    runs-on: ubuntu-latest
    environment: ${{ inputs.environment }}
    needs: metadata
    strategy:
      fail-fast: false
      matrix:
        service: [api, ingest, workers, web, migrations]

    steps:
      - name: Checkout
        uses: actions/checkout@v5
        with:
          ref: ${{ inputs.ref }}

      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 25

      - name: Login to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3

      - name: Build and push image
        uses: docker/build-push-action@v6
        with:
          context: .
          target: ${{ matrix.service }}
          push: true
          tags: |
            ${{ needs.metadata.outputs.registry }}/latitude-${{ inputs.environment }}-${{ matrix.service }}:${{ needs.metadata.outputs.image_tag }}
            ${{ needs.metadata.outputs.registry }}/latitude-${{ inputs.environment }}-${{ matrix.service }}:latest
          build-args: ${{ matrix.service == 'web' && format('VITE_LAT_API_URL={0}\nVITE_LAT_WEB_URL={1}', vars.VITE_LAT_API_URL, vars.VITE_LAT_WEB_URL) || '' }}
          cache-from: type=gha,scope=${{ matrix.service }}
          cache-to: type=gha,mode=max,scope=${{ matrix.service }}
