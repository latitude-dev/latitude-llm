name: Python Lint

on:
  workflow_call:

jobs:
  python-lint:
    runs-on: ubuntu-latest

    steps:
      - name: Skip during rewrite bootstrap
        run: echo "No Python packages in rewrite bootstrap yet"
