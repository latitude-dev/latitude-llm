{
  "version": "0.2.0",
  "configurations": [
    {
      "name": "Debug Web",
      "type": "node-terminal",
      "request": "launch",
      "command": "cd apps/web && pnpm dev",
      "serverReadyAction": {
        "pattern": "- Local:.+(https?://.+)",
        "uriFormat": "%s",
        "action": "debugWithChrome"
      }
    }
  ]
}
