import { BreadcrumbText } from "../../../../-components/breadcrumb-ui.tsx"

/**
 * Breadcrumb segment on the annotation queues list route (`annotation-queues/`).
 */
export function AqListBreadcrumb() {
  return <BreadcrumbText variant="muted">Annotation queues</BreadcrumbText>
}
