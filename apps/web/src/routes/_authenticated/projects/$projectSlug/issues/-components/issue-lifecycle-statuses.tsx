// The component lives in `apps/web/src/components/issues/` so it can be
// shared with the in-app notifications UI. This file re-exports it so the
// existing in-route imports keep working.
export { IssueLifecycleStatuses } from "../../../../../../components/issues/issue-lifecycle-statuses.tsx"
