{{- if and .Values.ingress.enabled .Values.ingress.hosts.websockets }}
apiVersion: networking.k8s.io/v1
kind: Ingress
metadata:
  name: {{ include "latitude.fullname" . }}-websockets
  labels:
    {{- include "latitude.labels" . | nindent 4 }}
    app.kubernetes.io/component: websockets
  {{- with .Values.ingress.annotations }}
  annotations:
    nginx.ingress.kubernetes.io/proxy-read-timeout: "3600"
    nginx.ingress.kubernetes.io/proxy-send-timeout: "3600"
    {{- toYaml . | nindent 4 }}
  {{- end }}
spec:
  {{- if .Values.ingress.className }}
  ingressClassName: {{ .Values.ingress.className }}
  {{- end }}
  {{- if .Values.ingress.tls.enabled }}
  tls:
    - hosts:
        - {{ .Values.ingress.hosts.websockets }}
      {{- if .Values.ingress.tls.secretName }}
      secretName: {{ .Values.ingress.tls.secretName }}
      {{- end }}
  {{- end }}
  rules:
    - host: {{ .Values.ingress.hosts.websockets }}
      http:
        paths:
          - path: /
            pathType: Prefix
            backend:
              service:
                name: {{ include "latitude.fullname" . }}-websockets
                port:
                  number: {{ .Values.service.websockets.port }}
{{- end }}
