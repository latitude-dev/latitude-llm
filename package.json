{
  "name": "latitude-llm",
  "private": true,
  "scripts": {
    "build": "turbo build",
    "dev": "turbo dev",
    "check": "turbo check",
    "hooks": "node ./scripts/setup-git-hooks.mjs",
    "format": "turbo format",
    "tmux": "tmuxinator start .",
    "typecheck": "turbo typecheck",
    "test": "turbo test",
    "knip": "knip",
    "knip:fix": "knip --fix",
    "knip:fix:files": "knip --fix-type files --allow-remove-files",
    "clean": "node ./scripts/clean-repo.mjs",
    "db:reset": "pnpm --filter \"@platform/db-postgres\" pg:reset && pnpm --filter \"@platform/db-clickhouse\" ch:reset",
    "db:up": "pnpm --filter \"@platform/db-postgres\" pg:migrate && pnpm --filter \"@platform/db-clickhouse\" ch:up && pnpm --filter \"@platform/db-weaviate\" wv:migrate",
    "seed": "pnpm --filter \"@platform/db-postgres\" pg:seed && pnpm --filter \"@platform/db-clickhouse\" ch:seed && pnpm --filter \"@platform/db-weaviate\" wv:seed",
    "migrate": "pnpm --filter \"@platform/db-postgres\" pg:migrate && pnpm --filter \"@platform/db-clickhouse\" ch:up && pnpm --filter \"@platform/db-weaviate\" wv:migrate",
    "reset": "pnpm clean && pnpm install && pnpm build && pnpm db:reset",
    "catchup": "pnpm clean && pnpm install && pnpm build && pnpm db:up",
    "openapi:emit": "pnpm --filter @app/api openapi:emit",
    "mcp:emit": "pnpm --filter @app/api mcp:emit",
    "mcp:inspect": "pnpm --filter @app/api mcp:inspect",
    "sdk:check": "./fern/invoke.sh check",
    "generate:sdk": "pnpm openapi:emit && pnpm sdk:check && ./fern/invoke.sh generate --group local --local --force",
    "docs:dev": "cd docs && pnpm dlx mint dev",
    "docs:check": "cd docs && pnpm dlx mint broken-links",
    "prepare": "pnpm hooks && effect-language-service patch"
  },
  "devDependencies": {
    "@biomejs/biome": "catalog:",
    "@effect/language-service": "0.84.3",
    "@types/node": "catalog:",
    "@typescript/native-preview": "catalog:",
    "fern-api": "4.80.1",
    "knip": "6.6.2",
    "turbo": "catalog:",
    "typescript": "catalog:",
    "vitest": "catalog:"
  },
  "packageManager": "pnpm@10.33.0",
  "engines": {
    "node": ">=25"
  },
  "version": "0.0.1",
  "pnpm": {
    "overrides": {
      "@ai-sdk/provider": "4.0.0-beta.7",
      "@opentelemetry/api": "1.9.1",
      "drizzle-orm": "1.0.0-beta.15-859cf75",
      "hono": "4.12.16",
      "tslib": "2.8.1"
    }
  }
}
