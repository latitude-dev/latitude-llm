# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.agent_score_causes import AgentScoreCauses
from ..types.agent_score_history import AgentScoreHistory
from ..types.current_agent_score import CurrentAgentScore
from .raw_client import AsyncRawAgentScoreClient, RawAgentScoreClient


class AgentScoreClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawAgentScoreClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawAgentScoreClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawAgentScoreClient
        """
        return self._raw_client

    def history(
        self,
        project_slug: str,
        *,
        from_: typing.Optional[dt.date] = None,
        to: typing.Optional[dt.date] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AgentScoreHistory:
        """
        Returns the project's published Agent Scores in a date range, oldest first. Days the project did not publish are absent from the list.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        from_ : typing.Optional[dt.date]
            Inclusive start date as `YYYY-MM-DD`. Defaults to 90 days before `to`.

        to : typing.Optional[dt.date]
            Inclusive end date as `YYYY-MM-DD`. Defaults to today.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentScoreHistory
            Published Agent Scores in the range

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.agent_score.history(
            project_slug="projectSlug",
        )
        """
        _response = self._raw_client.history(project_slug, from_=from_, to=to, request_options=request_options)
        return _response.data

    def causes(self, project_slug: str, *, request_options: typing.Optional[RequestOptions] = None) -> AgentScoreCauses:
        """
        Returns what explains the project's current Agent Score: ranked causes per dimension, and where Outcome failures and Safety harm concentrate. This is current evidence from the live window and does not reconstruct any stored score.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentScoreCauses
            Current cause rows, or an explicit absence

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.agent_score.causes(
            project_slug="projectSlug",
        )
        """
        _response = self._raw_client.causes(project_slug, request_options=request_options)
        return _response.data

    def get(self, project_slug: str, *, request_options: typing.Optional[RequestOptions] = None) -> CurrentAgentScore:
        """
        Returns the project's Agent Score for today: one number from 0 to 100 and the five dimensions behind it. A score is published only when every dimension meets its coverage and confidence floors, so a project can have no score for a day.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CurrentAgentScore
            Today's Agent Score, or an explicit absence

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.agent_score.get(
            project_slug="projectSlug",
        )
        """
        _response = self._raw_client.get(project_slug, request_options=request_options)
        return _response.data


class AsyncAgentScoreClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawAgentScoreClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawAgentScoreClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawAgentScoreClient
        """
        return self._raw_client

    async def history(
        self,
        project_slug: str,
        *,
        from_: typing.Optional[dt.date] = None,
        to: typing.Optional[dt.date] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> AgentScoreHistory:
        """
        Returns the project's published Agent Scores in a date range, oldest first. Days the project did not publish are absent from the list.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        from_ : typing.Optional[dt.date]
            Inclusive start date as `YYYY-MM-DD`. Defaults to 90 days before `to`.

        to : typing.Optional[dt.date]
            Inclusive end date as `YYYY-MM-DD`. Defaults to today.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentScoreHistory
            Published Agent Scores in the range

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agent_score.history(
                project_slug="projectSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.history(project_slug, from_=from_, to=to, request_options=request_options)
        return _response.data

    async def causes(
        self, project_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> AgentScoreCauses:
        """
        Returns what explains the project's current Agent Score: ranked causes per dimension, and where Outcome failures and Safety harm concentrate. This is current evidence from the live window and does not reconstruct any stored score.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        AgentScoreCauses
            Current cause rows, or an explicit absence

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agent_score.causes(
                project_slug="projectSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.causes(project_slug, request_options=request_options)
        return _response.data

    async def get(
        self, project_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> CurrentAgentScore:
        """
        Returns the project's Agent Score for today: one number from 0 to 100 and the five dimensions behind it. A score is published only when every dimension meets its coverage and confidence floors, so a project can have no score for a day.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        CurrentAgentScore
            Today's Agent Score, or an explicit absence

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.agent_score.get(
                project_slug="projectSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(project_slug, request_options=request_options)
        return _response.data
