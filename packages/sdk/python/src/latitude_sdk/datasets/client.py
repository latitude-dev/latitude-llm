# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.dataset import Dataset
from ..types.dataset_column import DatasetColumn
from ..types.dataset_columns_list import DatasetColumnsList
from ..types.delete_dataset_rows_response import DeleteDatasetRowsResponse
from ..types.export_dataset_rows_ready_response import ExportDatasetRowsReadyResponse
from ..types.import_rows_from_traces_response import ImportRowsFromTracesResponse
from ..types.insert_dataset_rows_response import InsertDatasetRowsResponse
from ..types.paginated_dataset_rows import PaginatedDatasetRows
from ..types.paginated_datasets import PaginatedDatasets
from ..types.traces_ref import TracesRef
from .raw_client import AsyncRawDatasetsClient, RawDatasetsClient
from .types.datasets_list_columns_request_include_removed import DatasetsListColumnsRequestIncludeRemoved
from .types.datasets_list_request_sort_by import DatasetsListRequestSortBy
from .types.datasets_list_request_sort_direction import DatasetsListRequestSortDirection
from .types.datasets_list_rows_request_sort_direction import DatasetsListRowsRequestSortDirection
from .types.delete_dataset_rows_body_selection import DeleteDatasetRowsBodySelection
from .types.export_dataset_rows_body_selection import ExportDatasetRowsBodySelection
from .types.insert_dataset_rows_body_rows_item import InsertDatasetRowsBodyRowsItem

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class DatasetsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawDatasetsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawDatasetsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawDatasetsClient
        """
        return self._raw_client

    def list(
        self,
        project_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        sort_by: typing.Optional[DatasetsListRequestSortBy] = None,
        sort_direction: typing.Optional[DatasetsListRequestSortDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasets:
        """
        Returns a cursor-paginated page of datasets in the project.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        sort_by : typing.Optional[DatasetsListRequestSortBy]
            Field to sort by. Defaults to `updatedAt`.

        sort_direction : typing.Optional[DatasetsListRequestSortDirection]
            Sort direction. Defaults to `desc`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasets
            Page of datasets

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.list(
            project_slug="projectSlug",
            cursor="cursor",
            limit=1,
            sort_by="name",
            sort_direction="asc",
        )
        """
        _response = self._raw_client.list(
            project_slug,
            cursor=cursor,
            limit=limit,
            sort_by=sort_by,
            sort_direction=sort_direction,
            request_options=request_options,
        )
        return _response.data

    def create(
        self,
        project_slug: str,
        *,
        name: str,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Dataset:
        """
        Creates an empty dataset in the project. The slug is derived from `name`.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        name : str
            Human-readable name. Used to derive the slug.

        description : typing.Optional[str]
            Free-form description. Defaults to `null` when omitted or empty.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Dataset
            Created dataset

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.create(
            project_slug="projectSlug",
            name="name",
        )
        """
        _response = self._raw_client.create(
            project_slug, name=name, description=description, request_options=request_options
        )
        return _response.data

    def get(
        self, project_slug: str, dataset_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> Dataset:
        """
        Returns one dataset by slug.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Dataset
            Dataset

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.get(
            project_slug="projectSlug",
            dataset_slug="datasetSlug",
        )
        """
        _response = self._raw_client.get(project_slug, dataset_slug, request_options=request_options)
        return _response.data

    def delete(
        self, project_slug: str, dataset_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Deletes a dataset by slug.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.delete(
            project_slug="projectSlug",
            dataset_slug="datasetSlug",
        )
        """
        _response = self._raw_client.delete(project_slug, dataset_slug, request_options=request_options)
        return _response.data

    def update(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Dataset:
        """
        Updates a dataset's `name` and/or `description`. Renaming regenerates the slug — clients should re-read the response or rely on the `id` for stable references.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        name : typing.Optional[str]
            New human-readable name. Renaming regenerates the slug.

        description : typing.Optional[str]
            New description. Pass `null` to clear; omit to keep the current value.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Dataset
            Updated dataset

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.update(
            project_slug="projectSlug",
            dataset_slug="datasetSlug",
        )
        """
        _response = self._raw_client.update(
            project_slug, dataset_slug, name=name, description=description, request_options=request_options
        )
        return _response.data

    def list_rows(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        sort_direction: typing.Optional[DatasetsListRowsRequestSortDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasetRows:
        """
        Returns a cursor-paginated page of rows.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        search : typing.Optional[str]
            Free-text search against row cells.

        sort_direction : typing.Optional[DatasetsListRowsRequestSortDirection]
            Sort direction on `createdAt`. Defaults to `desc` (newest first).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasetRows
            Page of rows

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.list_rows(
            project_slug="projectSlug",
            dataset_slug="datasetSlug",
            cursor="cursor",
            limit=1,
            search="search",
            sort_direction="asc",
        )
        """
        _response = self._raw_client.list_rows(
            project_slug,
            dataset_slug,
            cursor=cursor,
            limit=limit,
            search=search,
            sort_direction=sort_direction,
            request_options=request_options,
        )
        return _response.data

    def insert_rows(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        rows: typing.Sequence[InsertDatasetRowsBodyRowsItem],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> InsertDatasetRowsResponse:
        """
        Appends one or more rows to the dataset.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        rows : typing.Sequence[InsertDatasetRowsBodyRowsItem]
            Rows to insert.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        InsertDatasetRowsResponse
            Rows inserted

        Examples
        --------
        from latitude import LatitudeApiClient
        from latitude.datasets import InsertDatasetRowsBodyRowsItem

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.insert_rows(
            project_slug="projectSlug",
            dataset_slug="datasetSlug",
            rows=[InsertDatasetRowsBodyRowsItem()],
        )
        """
        _response = self._raw_client.insert_rows(project_slug, dataset_slug, rows=rows, request_options=request_options)
        return _response.data

    def delete_rows(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        selection: DeleteDatasetRowsBodySelection,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeleteDatasetRowsResponse:
        """
        Deletes rows matching the supplied selection.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        selection : DeleteDatasetRowsBodySelection
            Rows to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteDatasetRowsResponse
            Rows deleted

        Examples
        --------
        from latitude import LatitudeApiClient
        from latitude.datasets import DeleteDatasetRowsBodySelection_Selected

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.delete_rows(
            project_slug="projectSlug",
            dataset_slug="datasetSlug",
            selection=DeleteDatasetRowsBodySelection_Selected(
                row_ids=["rowIds"],
            ),
        )
        """
        _response = self._raw_client.delete_rows(
            project_slug, dataset_slug, selection=selection, request_options=request_options
        )
        return _response.data

    def import_rows_from_traces(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        traces: TracesRef,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ImportRowsFromTracesResponse:
        """
        Imports one row per trace matched by `traces`.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        traces : TracesRef

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ImportRowsFromTracesResponse
            Rows imported

        Examples
        --------
        from latitude import LatitudeApiClient, TracesRef_Ids

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.import_rows_from_traces(
            project_slug="projectSlug",
            dataset_slug="datasetSlug",
            traces=TracesRef_Ids(
                ids=["ids"],
            ),
        )
        """
        _response = self._raw_client.import_rows_from_traces(
            project_slug, dataset_slug, traces=traces, request_options=request_options
        )
        return _response.data

    def export_rows(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        selection: typing.Optional[ExportDatasetRowsBodySelection] = OMIT,
        recipient: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ExportDatasetRowsReadyResponse:
        """
        Exports the selected rows as CSV. Returns one of three outcomes, discriminated by `status`:

        - `"ready"` — the export fit in the synchronous path. Body carries a short-lived signed `downloadUrl` the caller follows with a plain HTTP GET.
        - `"queued"` — the export was too large for the synchronous path AND a `recipient` was supplied. The CSV will be emailed to that address. The recipient must be a member of the requesting organization.
        - `"too_large"` — the export was too large for the synchronous path AND no `recipient` was supplied. Body includes a `recommendedAction` describing how to recover (typically: ask the user for an email and retry with `recipient` set).

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        selection : typing.Optional[ExportDatasetRowsBodySelection]
            Rows to export. Defaults to `{ mode: "all" }` when omitted.

        recipient : typing.Optional[str]
            Email address to send the download link to when the export is too large for the synchronous path. Must belong to a member of the requesting organization. Ignored when the export fits the synchronous path; required for the async email flow.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ExportDatasetRowsReadyResponse
            CSV ready at the signed URL

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.export_rows(
            project_slug="projectSlug",
            dataset_slug="datasetSlug",
        )
        """
        _response = self._raw_client.export_rows(
            project_slug, dataset_slug, selection=selection, recipient=recipient, request_options=request_options
        )
        return _response.data

    def list_columns(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        include_removed: typing.Optional[DatasetsListColumnsRequestIncludeRemoved] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetColumnsList:
        """
        Returns the ordered active column schema — the built-in columns plus any custom columns. Pass `includeRemoved=true` to also return soft-removed columns (so they can be restored).

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        include_removed : typing.Optional[DatasetsListColumnsRequestIncludeRemoved]
            When `true`, also returns soft-removed columns (each carrying `removed: true`). Defaults to `false`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetColumnsList
            Column schema

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.list_columns(
            project_slug="projectSlug",
            dataset_slug="datasetSlug",
            include_removed="true",
        )
        """
        _response = self._raw_client.list_columns(
            project_slug, dataset_slug, include_removed=include_removed, request_options=request_options
        )
        return _response.data

    def add_column(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        name: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetColumn:
        """
        Adds a custom column. The column starts empty on every row; rows are written only when a cell is filled, so the dataset version does not change.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        name : str
            Display name for the new custom column.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetColumn
            Created column

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.add_column(
            project_slug="projectSlug",
            dataset_slug="datasetSlug",
            name="name",
        )
        """
        _response = self._raw_client.add_column(project_slug, dataset_slug, name=name, request_options=request_options)
        return _response.data

    def delete_column(
        self,
        project_slug: str,
        dataset_slug: str,
        identifier: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Removes a column (built-in or custom) from the active schema. Its data is preserved and the column can be re-added; this does not change the dataset version.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        identifier : str
            Stable column identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.delete_column(
            project_slug="projectSlug",
            dataset_slug="datasetSlug",
            identifier="identifier",
        )
        """
        _response = self._raw_client.delete_column(
            project_slug, dataset_slug, identifier, request_options=request_options
        )
        return _response.data

    def update_column(
        self,
        project_slug: str,
        dataset_slug: str,
        identifier: str,
        *,
        name: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetColumn:
        """
        Renames a column. Works for both built-in and custom columns.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        identifier : str
            Stable column identifier.

        name : str
            New display name. Works for both built-in and custom columns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetColumn
            Updated column

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.update_column(
            project_slug="projectSlug",
            dataset_slug="datasetSlug",
            identifier="identifier",
            name="name",
        )
        """
        _response = self._raw_client.update_column(
            project_slug, dataset_slug, identifier, name=name, request_options=request_options
        )
        return _response.data

    def reorder_columns(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        order: typing.Sequence[str],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetColumnsList:
        """
        Sets the left-to-right order of columns. This is a metadata edit and does not change the dataset version.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        order : typing.Sequence[str]
            Column identifiers in the desired left-to-right order. Identifiers omitted from the list keep their relative order at the end; unknown identifiers are ignored.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetColumnsList
            Reordered column schema

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.reorder_columns(
            project_slug="projectSlug",
            dataset_slug="datasetSlug",
            order=["order"],
        )
        """
        _response = self._raw_client.reorder_columns(
            project_slug, dataset_slug, order=order, request_options=request_options
        )
        return _response.data

    def restore_column(
        self,
        project_slug: str,
        dataset_slug: str,
        identifier: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetColumn:
        """
        Restores a soft-removed column (built-in or custom) to the active schema, reconnecting its preserved data. Find removed identifiers via `listDatasetColumns` with `includeRemoved=true`.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        identifier : str
            Stable column identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetColumn
            Restored column

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.datasets.restore_column(
            project_slug="projectSlug",
            dataset_slug="datasetSlug",
            identifier="identifier",
        )
        """
        _response = self._raw_client.restore_column(
            project_slug, dataset_slug, identifier, request_options=request_options
        )
        return _response.data


class AsyncDatasetsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawDatasetsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawDatasetsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawDatasetsClient
        """
        return self._raw_client

    async def list(
        self,
        project_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        sort_by: typing.Optional[DatasetsListRequestSortBy] = None,
        sort_direction: typing.Optional[DatasetsListRequestSortDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasets:
        """
        Returns a cursor-paginated page of datasets in the project.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        sort_by : typing.Optional[DatasetsListRequestSortBy]
            Field to sort by. Defaults to `updatedAt`.

        sort_direction : typing.Optional[DatasetsListRequestSortDirection]
            Sort direction. Defaults to `desc`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasets
            Page of datasets

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.list(
                project_slug="projectSlug",
                cursor="cursor",
                limit=1,
                sort_by="name",
                sort_direction="asc",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            project_slug,
            cursor=cursor,
            limit=limit,
            sort_by=sort_by,
            sort_direction=sort_direction,
            request_options=request_options,
        )
        return _response.data

    async def create(
        self,
        project_slug: str,
        *,
        name: str,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Dataset:
        """
        Creates an empty dataset in the project. The slug is derived from `name`.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        name : str
            Human-readable name. Used to derive the slug.

        description : typing.Optional[str]
            Free-form description. Defaults to `null` when omitted or empty.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Dataset
            Created dataset

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.create(
                project_slug="projectSlug",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            project_slug, name=name, description=description, request_options=request_options
        )
        return _response.data

    async def get(
        self, project_slug: str, dataset_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> Dataset:
        """
        Returns one dataset by slug.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Dataset
            Dataset

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.get(
                project_slug="projectSlug",
                dataset_slug="datasetSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(project_slug, dataset_slug, request_options=request_options)
        return _response.data

    async def delete(
        self, project_slug: str, dataset_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Deletes a dataset by slug.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.delete(
                project_slug="projectSlug",
                dataset_slug="datasetSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(project_slug, dataset_slug, request_options=request_options)
        return _response.data

    async def update(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Dataset:
        """
        Updates a dataset's `name` and/or `description`. Renaming regenerates the slug — clients should re-read the response or rely on the `id` for stable references.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        name : typing.Optional[str]
            New human-readable name. Renaming regenerates the slug.

        description : typing.Optional[str]
            New description. Pass `null` to clear; omit to keep the current value.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Dataset
            Updated dataset

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.update(
                project_slug="projectSlug",
                dataset_slug="datasetSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            project_slug, dataset_slug, name=name, description=description, request_options=request_options
        )
        return _response.data

    async def list_rows(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        sort_direction: typing.Optional[DatasetsListRowsRequestSortDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedDatasetRows:
        """
        Returns a cursor-paginated page of rows.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        search : typing.Optional[str]
            Free-text search against row cells.

        sort_direction : typing.Optional[DatasetsListRowsRequestSortDirection]
            Sort direction on `createdAt`. Defaults to `desc` (newest first).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedDatasetRows
            Page of rows

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.list_rows(
                project_slug="projectSlug",
                dataset_slug="datasetSlug",
                cursor="cursor",
                limit=1,
                search="search",
                sort_direction="asc",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_rows(
            project_slug,
            dataset_slug,
            cursor=cursor,
            limit=limit,
            search=search,
            sort_direction=sort_direction,
            request_options=request_options,
        )
        return _response.data

    async def insert_rows(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        rows: typing.Sequence[InsertDatasetRowsBodyRowsItem],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> InsertDatasetRowsResponse:
        """
        Appends one or more rows to the dataset.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        rows : typing.Sequence[InsertDatasetRowsBodyRowsItem]
            Rows to insert.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        InsertDatasetRowsResponse
            Rows inserted

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient
        from latitude.datasets import InsertDatasetRowsBodyRowsItem

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.insert_rows(
                project_slug="projectSlug",
                dataset_slug="datasetSlug",
                rows=[InsertDatasetRowsBodyRowsItem()],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.insert_rows(
            project_slug, dataset_slug, rows=rows, request_options=request_options
        )
        return _response.data

    async def delete_rows(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        selection: DeleteDatasetRowsBodySelection,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DeleteDatasetRowsResponse:
        """
        Deletes rows matching the supplied selection.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        selection : DeleteDatasetRowsBodySelection
            Rows to delete.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DeleteDatasetRowsResponse
            Rows deleted

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient
        from latitude.datasets import DeleteDatasetRowsBodySelection_Selected

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.delete_rows(
                project_slug="projectSlug",
                dataset_slug="datasetSlug",
                selection=DeleteDatasetRowsBodySelection_Selected(
                    row_ids=["rowIds"],
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_rows(
            project_slug, dataset_slug, selection=selection, request_options=request_options
        )
        return _response.data

    async def import_rows_from_traces(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        traces: TracesRef,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ImportRowsFromTracesResponse:
        """
        Imports one row per trace matched by `traces`.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        traces : TracesRef

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ImportRowsFromTracesResponse
            Rows imported

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient, TracesRef_Ids

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.import_rows_from_traces(
                project_slug="projectSlug",
                dataset_slug="datasetSlug",
                traces=TracesRef_Ids(
                    ids=["ids"],
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.import_rows_from_traces(
            project_slug, dataset_slug, traces=traces, request_options=request_options
        )
        return _response.data

    async def export_rows(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        selection: typing.Optional[ExportDatasetRowsBodySelection] = OMIT,
        recipient: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ExportDatasetRowsReadyResponse:
        """
        Exports the selected rows as CSV. Returns one of three outcomes, discriminated by `status`:

        - `"ready"` — the export fit in the synchronous path. Body carries a short-lived signed `downloadUrl` the caller follows with a plain HTTP GET.
        - `"queued"` — the export was too large for the synchronous path AND a `recipient` was supplied. The CSV will be emailed to that address. The recipient must be a member of the requesting organization.
        - `"too_large"` — the export was too large for the synchronous path AND no `recipient` was supplied. Body includes a `recommendedAction` describing how to recover (typically: ask the user for an email and retry with `recipient` set).

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        selection : typing.Optional[ExportDatasetRowsBodySelection]
            Rows to export. Defaults to `{ mode: "all" }` when omitted.

        recipient : typing.Optional[str]
            Email address to send the download link to when the export is too large for the synchronous path. Must belong to a member of the requesting organization. Ignored when the export fits the synchronous path; required for the async email flow.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ExportDatasetRowsReadyResponse
            CSV ready at the signed URL

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.export_rows(
                project_slug="projectSlug",
                dataset_slug="datasetSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.export_rows(
            project_slug, dataset_slug, selection=selection, recipient=recipient, request_options=request_options
        )
        return _response.data

    async def list_columns(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        include_removed: typing.Optional[DatasetsListColumnsRequestIncludeRemoved] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetColumnsList:
        """
        Returns the ordered active column schema — the built-in columns plus any custom columns. Pass `includeRemoved=true` to also return soft-removed columns (so they can be restored).

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        include_removed : typing.Optional[DatasetsListColumnsRequestIncludeRemoved]
            When `true`, also returns soft-removed columns (each carrying `removed: true`). Defaults to `false`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetColumnsList
            Column schema

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.list_columns(
                project_slug="projectSlug",
                dataset_slug="datasetSlug",
                include_removed="true",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_columns(
            project_slug, dataset_slug, include_removed=include_removed, request_options=request_options
        )
        return _response.data

    async def add_column(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        name: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetColumn:
        """
        Adds a custom column. The column starts empty on every row; rows are written only when a cell is filled, so the dataset version does not change.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        name : str
            Display name for the new custom column.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetColumn
            Created column

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.add_column(
                project_slug="projectSlug",
                dataset_slug="datasetSlug",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.add_column(
            project_slug, dataset_slug, name=name, request_options=request_options
        )
        return _response.data

    async def delete_column(
        self,
        project_slug: str,
        dataset_slug: str,
        identifier: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> None:
        """
        Removes a column (built-in or custom) from the active schema. Its data is preserved and the column can be re-added; this does not change the dataset version.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        identifier : str
            Stable column identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.delete_column(
                project_slug="projectSlug",
                dataset_slug="datasetSlug",
                identifier="identifier",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete_column(
            project_slug, dataset_slug, identifier, request_options=request_options
        )
        return _response.data

    async def update_column(
        self,
        project_slug: str,
        dataset_slug: str,
        identifier: str,
        *,
        name: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetColumn:
        """
        Renames a column. Works for both built-in and custom columns.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        identifier : str
            Stable column identifier.

        name : str
            New display name. Works for both built-in and custom columns.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetColumn
            Updated column

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.update_column(
                project_slug="projectSlug",
                dataset_slug="datasetSlug",
                identifier="identifier",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_column(
            project_slug, dataset_slug, identifier, name=name, request_options=request_options
        )
        return _response.data

    async def reorder_columns(
        self,
        project_slug: str,
        dataset_slug: str,
        *,
        order: typing.Sequence[str],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetColumnsList:
        """
        Sets the left-to-right order of columns. This is a metadata edit and does not change the dataset version.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        order : typing.Sequence[str]
            Column identifiers in the desired left-to-right order. Identifiers omitted from the list keep their relative order at the end; unknown identifiers are ignored.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetColumnsList
            Reordered column schema

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.reorder_columns(
                project_slug="projectSlug",
                dataset_slug="datasetSlug",
                order=["order"],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.reorder_columns(
            project_slug, dataset_slug, order=order, request_options=request_options
        )
        return _response.data

    async def restore_column(
        self,
        project_slug: str,
        dataset_slug: str,
        identifier: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> DatasetColumn:
        """
        Restores a soft-removed column (built-in or custom) to the active schema, reconnecting its preserved data. Find removed identifiers via `listDatasetColumns` with `includeRemoved=true`.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        dataset_slug : str
            Dataset slug (human-readable identifier within the project).

        identifier : str
            Stable column identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        DatasetColumn
            Restored column

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.datasets.restore_column(
                project_slug="projectSlug",
                dataset_slug="datasetSlug",
                identifier="identifier",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.restore_column(
            project_slug, dataset_slug, identifier, request_options=request_options
        )
        return _response.data
