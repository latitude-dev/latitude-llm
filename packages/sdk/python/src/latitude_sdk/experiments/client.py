# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.experiment import Experiment
from ..types.experiment_comparison import ExperimentComparison
from ..types.paginated_experiments import PaginatedExperiments
from .raw_client import AsyncRawExperimentsClient, RawExperimentsClient
from .types.create_experiment_body_variants_item import CreateExperimentBodyVariantsItem
from .types.update_experiment_body_variants_item import UpdateExperimentBodyVariantsItem

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ExperimentsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawExperimentsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawExperimentsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawExperimentsClient
        """
        return self._raw_client

    def list(
        self,
        project_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedExperiments:
        """
        Returns the project's experiments with cheap summary metrics (variant count, distinct sessions and users across all variant populations). Excludes per-variant comparison metrics — fetch a single experiment for those.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 100.

        search : typing.Optional[str]
            Filter by name (case-insensitive substring).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedExperiments
            Page of experiments

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.experiments.list(
            project_slug="projectSlug",
        )
        """
        _response = self._raw_client.list(
            project_slug, cursor=cursor, limit=limit, search=search, request_options=request_options
        )
        return _response.data

    def create(
        self,
        project_slug: str,
        *,
        name: str,
        description: typing.Optional[str] = OMIT,
        variants: typing.Optional[typing.Sequence[CreateExperimentBodyVariantsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Experiment:
        """
        Creates an experiment. The slug is derived from `name`. Omit `variants` to seed two defaults.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        name : str
            Human-readable name. Used to derive the slug.

        description : typing.Optional[str]
            Optional free-form description.

        variants : typing.Optional[typing.Sequence[CreateExperimentBodyVariantsItem]]
            Variant definitions. Omit to seed two default variants (`Variant A` baseline + `Variant B`); pass `[]` to create an empty experiment.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Experiment
            Experiment created

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.experiments.create(
            project_slug="projectSlug",
            name="name",
        )
        """
        _response = self._raw_client.create(
            project_slug, name=name, description=description, variants=variants, request_options=request_options
        )
        return _response.data

    def get(
        self, project_slug: str, experiment_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ExperimentComparison:
        """
        Returns a single experiment plus its comparison: per-variant metrics, deltas vs the baseline, and population-deviation flags.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        experiment_slug : str
            Experiment slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ExperimentComparison
            Experiment comparison

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.experiments.get(
            project_slug="projectSlug",
            experiment_slug="experimentSlug",
        )
        """
        _response = self._raw_client.get(project_slug, experiment_slug, request_options=request_options)
        return _response.data

    def update(
        self,
        project_slug: str,
        experiment_slug: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        variants: typing.Optional[typing.Sequence[UpdateExperimentBodyVariantsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Experiment:
        """
        Replaces an experiment's mutable fields. `variants`, when supplied, fully replaces the array (each variant carries its own `baseline` flag). Renaming may regenerate the slug.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        experiment_slug : str
            Experiment slug (human-readable identifier within the project).

        name : typing.Optional[str]
            New name. Renaming may regenerate the slug — re-read the response or rely on `id`.

        description : typing.Optional[str]
            New description.

        variants : typing.Optional[typing.Sequence[UpdateExperimentBodyVariantsItem]]
            Full replacement of the variants array. Each variant carries its own `baseline` flag.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Experiment
            Updated experiment

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.experiments.update(
            project_slug="projectSlug",
            experiment_slug="experimentSlug",
        )
        """
        _response = self._raw_client.update(
            project_slug,
            experiment_slug,
            name=name,
            description=description,
            variants=variants,
            request_options=request_options,
        )
        return _response.data

    def delete(
        self, project_slug: str, experiment_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Deletes an experiment. Its slug becomes reusable.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        experiment_slug : str
            Experiment slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.experiments.delete(
            project_slug="projectSlug",
            experiment_slug="experimentSlug",
        )
        """
        _response = self._raw_client.delete(project_slug, experiment_slug, request_options=request_options)
        return _response.data


class AsyncExperimentsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawExperimentsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawExperimentsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawExperimentsClient
        """
        return self._raw_client

    async def list(
        self,
        project_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedExperiments:
        """
        Returns the project's experiments with cheap summary metrics (variant count, distinct sessions and users across all variant populations). Excludes per-variant comparison metrics — fetch a single experiment for those.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 100.

        search : typing.Optional[str]
            Filter by name (case-insensitive substring).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedExperiments
            Page of experiments

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.experiments.list(
                project_slug="projectSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            project_slug, cursor=cursor, limit=limit, search=search, request_options=request_options
        )
        return _response.data

    async def create(
        self,
        project_slug: str,
        *,
        name: str,
        description: typing.Optional[str] = OMIT,
        variants: typing.Optional[typing.Sequence[CreateExperimentBodyVariantsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Experiment:
        """
        Creates an experiment. The slug is derived from `name`. Omit `variants` to seed two defaults.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        name : str
            Human-readable name. Used to derive the slug.

        description : typing.Optional[str]
            Optional free-form description.

        variants : typing.Optional[typing.Sequence[CreateExperimentBodyVariantsItem]]
            Variant definitions. Omit to seed two default variants (`Variant A` baseline + `Variant B`); pass `[]` to create an empty experiment.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Experiment
            Experiment created

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.experiments.create(
                project_slug="projectSlug",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            project_slug, name=name, description=description, variants=variants, request_options=request_options
        )
        return _response.data

    async def get(
        self, project_slug: str, experiment_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ExperimentComparison:
        """
        Returns a single experiment plus its comparison: per-variant metrics, deltas vs the baseline, and population-deviation flags.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        experiment_slug : str
            Experiment slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ExperimentComparison
            Experiment comparison

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.experiments.get(
                project_slug="projectSlug",
                experiment_slug="experimentSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(project_slug, experiment_slug, request_options=request_options)
        return _response.data

    async def update(
        self,
        project_slug: str,
        experiment_slug: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        variants: typing.Optional[typing.Sequence[UpdateExperimentBodyVariantsItem]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Experiment:
        """
        Replaces an experiment's mutable fields. `variants`, when supplied, fully replaces the array (each variant carries its own `baseline` flag). Renaming may regenerate the slug.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        experiment_slug : str
            Experiment slug (human-readable identifier within the project).

        name : typing.Optional[str]
            New name. Renaming may regenerate the slug — re-read the response or rely on `id`.

        description : typing.Optional[str]
            New description.

        variants : typing.Optional[typing.Sequence[UpdateExperimentBodyVariantsItem]]
            Full replacement of the variants array. Each variant carries its own `baseline` flag.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Experiment
            Updated experiment

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.experiments.update(
                project_slug="projectSlug",
                experiment_slug="experimentSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            project_slug,
            experiment_slug,
            name=name,
            description=description,
            variants=variants,
            request_options=request_options,
        )
        return _response.data

    async def delete(
        self, project_slug: str, experiment_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Deletes an experiment. Its slug becomes reusable.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        experiment_slug : str
            Experiment slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.experiments.delete(
                project_slug="projectSlug",
                experiment_slug="experimentSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(project_slug, experiment_slug, request_options=request_options)
        return _response.data
