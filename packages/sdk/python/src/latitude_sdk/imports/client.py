# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.import_ import Import
from ..types.import_credentials import ImportCredentials
from ..types.import_detail import ImportDetail
from ..types.list_imports_response import ListImportsResponse
from .raw_client import AsyncRawImportsClient, RawImportsClient
from .types.retry_import_body_credentials import RetryImportBodyCredentials

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class ImportsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawImportsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawImportsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawImportsClient
        """
        return self._raw_client

    def list(
        self, project_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ListImportsResponse:
        """
        Returns the project's imports from other observability platforms, newest first, without their per-page run history — fetch a single import for that.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListImportsResponse
            List of imports

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.imports.list(
            project_slug="projectSlug",
        )
        """
        _response = self._raw_client.list(project_slug, request_options=request_options)
        return _response.data

    def create(
        self,
        project_slug: str,
        *,
        credentials: ImportCredentials,
        source_project_id: str,
        source_project_name: typing.Optional[str] = OMIT,
        range_from: typing.Optional[dt.datetime] = OMIT,
        range_to: typing.Optional[dt.datetime] = OMIT,
        max_traces: typing.Optional[int] = OMIT,
        session_metadata_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Import:
        """
        Creates an import that copies historical traces from Langfuse, LangSmith, or Braintrust into the project, and starts it. Traces are imported newest first in the background; only one import runs at a time per organization.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        credentials : ImportCredentials

        source_project_id : str
            Id of the project on the platform to read from.

        source_project_name : typing.Optional[str]
            Name of the platform project, shown in Latitude. Defaults to `sourceProjectId`.

        range_from : typing.Optional[dt.datetime]
            ISO-8601 start of the range to import. Defaults to the plan's default lookback (90 days where retention allows) before `rangeTo`. Cannot reach past the plan's span retention.

        range_to : typing.Optional[dt.datetime]
            ISO-8601 end of the range to import. Defaults to now.

        max_traces : typing.Optional[int]
            Most traces to import, newest first. One imported trace bills one credit, like an ingested trace. Defaults to the maximum, 100,000.

        session_metadata_key : typing.Optional[str]
            LangSmith only: run metadata key that groups traces into sessions. Defaults to `thread_id`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Import
            Import created and started

        Examples
        --------
        from latitude_sdk import ImportCredentials_Langfuse, LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.imports.create(
            project_slug="projectSlug",
            credentials=ImportCredentials_Langfuse(
                region="eu",
                public_key="publicKey",
                secret_key="secretKey",
            ),
            source_project_id="sourceProjectId",
        )
        """
        _response = self._raw_client.create(
            project_slug,
            credentials=credentials,
            source_project_id=source_project_id,
            source_project_name=source_project_name,
            range_from=range_from,
            range_to=range_to,
            max_traces=max_traces,
            session_metadata_key=session_metadata_key,
            request_options=request_options,
        )
        return _response.data

    def get(
        self, project_slug: str, import_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ImportDetail:
        """
        Returns a single import, including the recent pages it processed.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        import_id : str
            Import id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ImportDetail
            Import

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.imports.get(
            project_slug="projectSlug",
            import_id="importId",
        )
        """
        _response = self._raw_client.get(project_slug, import_id, request_options=request_options)
        return _response.data

    def cancel(
        self, project_slug: str, import_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> Import:
        """
        Cancels an import that has not finished. Traces already imported stay, and the import can be retried later to carry on. Cancellation is cooperative, so a running import stops after the page in flight.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        import_id : str
            Import id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Import
            Import cancelled

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.imports.cancel(
            project_slug="projectSlug",
            import_id="importId",
        )
        """
        _response = self._raw_client.cancel(project_slug, import_id, request_options=request_options)
        return _response.data

    def retry(
        self,
        project_slug: str,
        import_id: str,
        *,
        credentials: RetryImportBodyCredentials,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Import:
        """
        Resumes a failed, cancelled, or capped import from where it stopped. Returns a new import carrying the original's counts forward; the original is kept as a record.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        import_id : str
            Import id.

        credentials : RetryImportBodyCredentials
            Platform credentials, provided again because Latitude discards them when an import ends. Must name the same region the import was created against.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Import
            Retry created and started

        Examples
        --------
        from latitude_sdk import LatitudeClient
        from latitude_sdk.imports import RetryImportBodyCredentials_Langfuse

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.imports.retry(
            project_slug="projectSlug",
            import_id="importId",
            credentials=RetryImportBodyCredentials_Langfuse(
                region="eu",
                public_key="publicKey",
                secret_key="secretKey",
            ),
        )
        """
        _response = self._raw_client.retry(
            project_slug, import_id, credentials=credentials, request_options=request_options
        )
        return _response.data


class AsyncImportsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawImportsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawImportsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawImportsClient
        """
        return self._raw_client

    async def list(
        self, project_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ListImportsResponse:
        """
        Returns the project's imports from other observability platforms, newest first, without their per-page run history — fetch a single import for that.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ListImportsResponse
            List of imports

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.imports.list(
                project_slug="projectSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(project_slug, request_options=request_options)
        return _response.data

    async def create(
        self,
        project_slug: str,
        *,
        credentials: ImportCredentials,
        source_project_id: str,
        source_project_name: typing.Optional[str] = OMIT,
        range_from: typing.Optional[dt.datetime] = OMIT,
        range_to: typing.Optional[dt.datetime] = OMIT,
        max_traces: typing.Optional[int] = OMIT,
        session_metadata_key: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Import:
        """
        Creates an import that copies historical traces from Langfuse, LangSmith, or Braintrust into the project, and starts it. Traces are imported newest first in the background; only one import runs at a time per organization.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        credentials : ImportCredentials

        source_project_id : str
            Id of the project on the platform to read from.

        source_project_name : typing.Optional[str]
            Name of the platform project, shown in Latitude. Defaults to `sourceProjectId`.

        range_from : typing.Optional[dt.datetime]
            ISO-8601 start of the range to import. Defaults to the plan's default lookback (90 days where retention allows) before `rangeTo`. Cannot reach past the plan's span retention.

        range_to : typing.Optional[dt.datetime]
            ISO-8601 end of the range to import. Defaults to now.

        max_traces : typing.Optional[int]
            Most traces to import, newest first. One imported trace bills one credit, like an ingested trace. Defaults to the maximum, 100,000.

        session_metadata_key : typing.Optional[str]
            LangSmith only: run metadata key that groups traces into sessions. Defaults to `thread_id`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Import
            Import created and started

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient, ImportCredentials_Langfuse

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.imports.create(
                project_slug="projectSlug",
                credentials=ImportCredentials_Langfuse(
                    region="eu",
                    public_key="publicKey",
                    secret_key="secretKey",
                ),
                source_project_id="sourceProjectId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            project_slug,
            credentials=credentials,
            source_project_id=source_project_id,
            source_project_name=source_project_name,
            range_from=range_from,
            range_to=range_to,
            max_traces=max_traces,
            session_metadata_key=session_metadata_key,
            request_options=request_options,
        )
        return _response.data

    async def get(
        self, project_slug: str, import_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> ImportDetail:
        """
        Returns a single import, including the recent pages it processed.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        import_id : str
            Import id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ImportDetail
            Import

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.imports.get(
                project_slug="projectSlug",
                import_id="importId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(project_slug, import_id, request_options=request_options)
        return _response.data

    async def cancel(
        self, project_slug: str, import_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> Import:
        """
        Cancels an import that has not finished. Traces already imported stay, and the import can be retried later to carry on. Cancellation is cooperative, so a running import stops after the page in flight.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        import_id : str
            Import id.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Import
            Import cancelled

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.imports.cancel(
                project_slug="projectSlug",
                import_id="importId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.cancel(project_slug, import_id, request_options=request_options)
        return _response.data

    async def retry(
        self,
        project_slug: str,
        import_id: str,
        *,
        credentials: RetryImportBodyCredentials,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Import:
        """
        Resumes a failed, cancelled, or capped import from where it stopped. Returns a new import carrying the original's counts forward; the original is kept as a record.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        import_id : str
            Import id.

        credentials : RetryImportBodyCredentials
            Platform credentials, provided again because Latitude discards them when an import ends. Must name the same region the import was created against.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Import
            Retry created and started

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient
        from latitude_sdk.imports import RetryImportBodyCredentials_Langfuse

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.imports.retry(
                project_slug="projectSlug",
                import_id="importId",
                credentials=RetryImportBodyCredentials_Langfuse(
                    region="eu",
                    public_key="publicKey",
                    secret_key="secretKey",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.retry(
            project_slug, import_id, credentials=credentials, request_options=request_options
        )
        return _response.data
