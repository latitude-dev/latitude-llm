# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.export_issues_response import ExportIssuesResponse
from ..types.issue_analytics_response import IssueAnalyticsResponse
from ..types.issue_detail import IssueDetail
from ..types.issue_histogram import IssueHistogram
from ..types.issues_lifecycle_response import IssuesLifecycleResponse
from ..types.monitor_issue_response import MonitorIssueResponse
from ..types.paginated_issues import PaginatedIssues
from ..types.paginated_traces import PaginatedTraces
from .raw_client import AsyncRawIssuesClient, RawIssuesClient
from .types.export_issues_body_lifecycle_group import ExportIssuesBodyLifecycleGroup
from .types.issues_list_request_lifecycle_group import IssuesListRequestLifecycleGroup
from .types.issues_list_request_sort_by import IssuesListRequestSortBy
from .types.issues_list_request_sort_direction import IssuesListRequestSortDirection

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class IssuesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawIssuesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawIssuesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawIssuesClient
        """
        return self._raw_client

    def list(
        self,
        project_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        query: typing.Optional[str] = None,
        lifecycle_group: typing.Optional[IssuesListRequestLifecycleGroup] = None,
        sort_by: typing.Optional[IssuesListRequestSortBy] = None,
        sort_direction: typing.Optional[IssuesListRequestSortDirection] = None,
        from_iso: typing.Optional[dt.datetime] = None,
        to_iso: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedIssues:
        """
        Returns a cursor-paginated page of issues in the project. Each item includes lifecycle `states` plus time-window stats: `firstSeenAt`, `lastSeenAt`, `occurrences`, `affectedTracesPercent`, `trend`, and `tags`.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        query : typing.Optional[str]
            Free-text semantic search across the issues' names and descriptions.

        lifecycle_group : typing.Optional[IssuesListRequestLifecycleGroup]
            `"active"` for unresolved/unignored issues; `"archived"` for the rest. Omit to include both.

        sort_by : typing.Optional[IssuesListRequestSortBy]
            Sort field. `lastSeen` orders by most recent occurrence; `occurrences` by total count in the time window; `state` by lifecycle priority.

        sort_direction : typing.Optional[IssuesListRequestSortDirection]
            Sort direction. Defaults to `desc`.

        from_iso : typing.Optional[dt.datetime]
            Lower bound (inclusive) of the time window. Defaults to ~6 days ago.

        to_iso : typing.Optional[dt.datetime]
            Upper bound (inclusive) of the time window. Defaults to now.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedIssues
            Page of issues

        Examples
        --------
        import datetime

        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.issues.list(
            project_slug="projectSlug",
            cursor="cursor",
            limit=1,
            query="query",
            lifecycle_group="active",
            sort_by="lastSeen",
            sort_direction="asc",
            from_iso=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
            to_iso=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
        )
        """
        _response = self._raw_client.list(
            project_slug,
            cursor=cursor,
            limit=limit,
            query=query,
            lifecycle_group=lifecycle_group,
            sort_by=sort_by,
            sort_direction=sort_direction,
            from_iso=from_iso,
            to_iso=to_iso,
            request_options=request_options,
        )
        return _response.data

    def analytics(
        self,
        project_slug: str,
        *,
        from_iso: typing.Optional[dt.datetime] = None,
        to_iso: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IssueAnalyticsResponse:
        """
        Returns issue analytics for the project: counts of ongoing, new, escalating, regressed, and resolved issues, plus total occurrences and a per-bucket occurrence series. Buckets are 12-hour UTC-aligned. The range defaults to the trailing 7 days.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        from_iso : typing.Optional[dt.datetime]
            Lower bound (inclusive) of the time range. Defaults to 7 days before `toIso`.

        to_iso : typing.Optional[dt.datetime]
            Upper bound (inclusive) of the time range. Defaults to now.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IssueAnalyticsResponse
            Issue analytics

        Examples
        --------
        import datetime

        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.issues.analytics(
            project_slug="projectSlug",
            from_iso=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
            to_iso=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
        )
        """
        _response = self._raw_client.analytics(
            project_slug, from_iso=from_iso, to_iso=to_iso, request_options=request_options
        )
        return _response.data

    def get(
        self, project_slug: str, issue_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> IssueDetail:
        """
        Returns the full-history detail view of one issue: lifecycle `states`, lifetime activity stats (`firstSeenAt`, `lastSeenAt`, `occurrences`, `affectedTracesPercent`, `tags`), a 14-day occurrence `trend`, the active `evaluations` monitoring it, and the current `monitoringState`.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_slug : str
            Issue slug.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IssueDetail
            Issue

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.issues.get(
            project_slug="projectSlug",
            issue_slug="issueSlug",
        )
        """
        _response = self._raw_client.get(project_slug, issue_slug, request_options=request_options)
        return _response.data

    def trend(
        self,
        project_slug: str,
        issue_slug: str,
        *,
        from_iso: typing.Optional[dt.datetime] = None,
        to_iso: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IssueHistogram:
        """
        Returns the occurrence histogram for one issue over `[fromIso, toIso]`. The default range is the trailing 14 days. Buckets are 12-hour wide and UTC-aligned.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_slug : str
            Issue slug.

        from_iso : typing.Optional[dt.datetime]
            Lower bound (inclusive). Defaults to ~14 days before `toIso`.

        to_iso : typing.Optional[dt.datetime]
            Upper bound (inclusive). Defaults to now.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IssueHistogram
            Occurrence histogram

        Examples
        --------
        import datetime

        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.issues.trend(
            project_slug="projectSlug",
            issue_slug="issueSlug",
            from_iso=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
            to_iso=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
        )
        """
        _response = self._raw_client.trend(
            project_slug, issue_slug, from_iso=from_iso, to_iso=to_iso, request_options=request_options
        )
        return _response.data

    def list_traces(
        self,
        project_slug: str,
        issue_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedTraces:
        """
        Returns the page of distinct traces that contributed at least one occurrence of the issue, ordered by most recent activity first.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_slug : str
            Issue slug.

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedTraces
            Page of traces

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.issues.list_traces(
            project_slug="projectSlug",
            issue_slug="issueSlug",
            cursor="cursor",
            limit=1,
        )
        """
        _response = self._raw_client.list_traces(
            project_slug, issue_slug, cursor=cursor, limit=limit, request_options=request_options
        )
        return _response.data

    def resolve(
        self,
        project_slug: str,
        *,
        issue_ids: typing.Sequence[str],
        keep_monitoring: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IssuesLifecycleResponse:
        """
        Marks each issue in `issueIds` as resolved. When `keepMonitoring` is `false`, monitoring is also stopped for each resolved issue; when omitted, the project's default applies.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_ids : typing.Sequence[str]
            Non-empty list of issue ids. Operations are idempotent — already-applied issues are unchanged.

        keep_monitoring : typing.Optional[bool]
            When `true`, monitoring continues after the issues are resolved. When `false`, monitoring stops. Defaults to the project setting.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IssuesLifecycleResponse
            Per-issue result

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.issues.resolve(
            project_slug="projectSlug",
            issue_ids=["issueIds"],
        )
        """
        _response = self._raw_client.resolve(
            project_slug, issue_ids=issue_ids, keep_monitoring=keep_monitoring, request_options=request_options
        )
        return _response.data

    def unresolve(
        self,
        project_slug: str,
        *,
        issue_ids: typing.Sequence[str],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IssuesLifecycleResponse:
        """
        Reverts each issue in `issueIds` to the unresolved state.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_ids : typing.Sequence[str]
            Non-empty list of issue ids. Operations are idempotent — already-applied issues are unchanged.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IssuesLifecycleResponse
            Per-issue result

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.issues.unresolve(
            project_slug="projectSlug",
            issue_ids=["issueIds"],
        )
        """
        _response = self._raw_client.unresolve(project_slug, issue_ids=issue_ids, request_options=request_options)
        return _response.data

    def ignore(
        self,
        project_slug: str,
        *,
        issue_ids: typing.Sequence[str],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IssuesLifecycleResponse:
        """
        Marks each issue in `issueIds` as ignored. Monitoring is also stopped for each ignored issue.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_ids : typing.Sequence[str]
            Non-empty list of issue ids. Operations are idempotent — already-applied issues are unchanged.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IssuesLifecycleResponse
            Per-issue result

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.issues.ignore(
            project_slug="projectSlug",
            issue_ids=["issueIds"],
        )
        """
        _response = self._raw_client.ignore(project_slug, issue_ids=issue_ids, request_options=request_options)
        return _response.data

    def unignore(
        self,
        project_slug: str,
        *,
        issue_ids: typing.Sequence[str],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IssuesLifecycleResponse:
        """
        Reverts each issue in `issueIds` to a non-ignored state.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_ids : typing.Sequence[str]
            Non-empty list of issue ids. Operations are idempotent — already-applied issues are unchanged.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IssuesLifecycleResponse
            Per-issue result

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.issues.unignore(
            project_slug="projectSlug",
            issue_ids=["issueIds"],
        )
        """
        _response = self._raw_client.unignore(project_slug, issue_ids=issue_ids, request_options=request_options)
        return _response.data

    def monitor(
        self, project_slug: str, issue_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> MonitorIssueResponse:
        """
        Starts (or realigns) monitoring for the issue. When the issue has no active evaluation, a new one is generated. When an active evaluation exists, the call realigns it. The work runs asynchronously and the response returns immediately. Returns 400 when monitoring is already in progress for this issue.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_slug : str
            Issue slug.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MonitorIssueResponse
            Monitor job enqueued

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.issues.monitor(
            project_slug="projectSlug",
            issue_slug="issueSlug",
        )
        """
        _response = self._raw_client.monitor(project_slug, issue_slug, request_options=request_options)
        return _response.data

    def unmonitor(
        self, project_slug: str, issue_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Stops monitoring the issue. Idempotent — issues that aren't being monitored return 204 without changing anything.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_slug : str
            Issue slug.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.issues.unmonitor(
            project_slug="projectSlug",
            issue_slug="issueSlug",
        )
        """
        _response = self._raw_client.unmonitor(project_slug, issue_slug, request_options=request_options)
        return _response.data

    def export(
        self,
        project_slug: str,
        *,
        recipient: str,
        issue_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        lifecycle_group: typing.Optional[ExportIssuesBodyLifecycleGroup] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ExportIssuesResponse:
        """
        Enqueues an asynchronous CSV export. The response returns immediately; the download link is emailed to `recipient` when the file is ready. The recipient must be a member of the requesting organization.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        recipient : str
            Email address the download link is sent to. Must belong to a member of the requesting organization.

        issue_ids : typing.Optional[typing.Sequence[str]]
            Restrict the export to this subset of issues. Omit to export every issue in the project.

        lifecycle_group : typing.Optional[ExportIssuesBodyLifecycleGroup]
            `"active"` for unresolved/unignored issues; `"archived"` for the rest. Omit to include both.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ExportIssuesResponse
            Export enqueued

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.issues.export(
            project_slug="projectSlug",
            recipient="recipient",
        )
        """
        _response = self._raw_client.export(
            project_slug,
            recipient=recipient,
            issue_ids=issue_ids,
            lifecycle_group=lifecycle_group,
            request_options=request_options,
        )
        return _response.data


class AsyncIssuesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawIssuesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawIssuesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawIssuesClient
        """
        return self._raw_client

    async def list(
        self,
        project_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        query: typing.Optional[str] = None,
        lifecycle_group: typing.Optional[IssuesListRequestLifecycleGroup] = None,
        sort_by: typing.Optional[IssuesListRequestSortBy] = None,
        sort_direction: typing.Optional[IssuesListRequestSortDirection] = None,
        from_iso: typing.Optional[dt.datetime] = None,
        to_iso: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedIssues:
        """
        Returns a cursor-paginated page of issues in the project. Each item includes lifecycle `states` plus time-window stats: `firstSeenAt`, `lastSeenAt`, `occurrences`, `affectedTracesPercent`, `trend`, and `tags`.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        query : typing.Optional[str]
            Free-text semantic search across the issues' names and descriptions.

        lifecycle_group : typing.Optional[IssuesListRequestLifecycleGroup]
            `"active"` for unresolved/unignored issues; `"archived"` for the rest. Omit to include both.

        sort_by : typing.Optional[IssuesListRequestSortBy]
            Sort field. `lastSeen` orders by most recent occurrence; `occurrences` by total count in the time window; `state` by lifecycle priority.

        sort_direction : typing.Optional[IssuesListRequestSortDirection]
            Sort direction. Defaults to `desc`.

        from_iso : typing.Optional[dt.datetime]
            Lower bound (inclusive) of the time window. Defaults to ~6 days ago.

        to_iso : typing.Optional[dt.datetime]
            Upper bound (inclusive) of the time window. Defaults to now.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedIssues
            Page of issues

        Examples
        --------
        import asyncio
        import datetime

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.issues.list(
                project_slug="projectSlug",
                cursor="cursor",
                limit=1,
                query="query",
                lifecycle_group="active",
                sort_by="lastSeen",
                sort_direction="asc",
                from_iso=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
                to_iso=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            project_slug,
            cursor=cursor,
            limit=limit,
            query=query,
            lifecycle_group=lifecycle_group,
            sort_by=sort_by,
            sort_direction=sort_direction,
            from_iso=from_iso,
            to_iso=to_iso,
            request_options=request_options,
        )
        return _response.data

    async def analytics(
        self,
        project_slug: str,
        *,
        from_iso: typing.Optional[dt.datetime] = None,
        to_iso: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IssueAnalyticsResponse:
        """
        Returns issue analytics for the project: counts of ongoing, new, escalating, regressed, and resolved issues, plus total occurrences and a per-bucket occurrence series. Buckets are 12-hour UTC-aligned. The range defaults to the trailing 7 days.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        from_iso : typing.Optional[dt.datetime]
            Lower bound (inclusive) of the time range. Defaults to 7 days before `toIso`.

        to_iso : typing.Optional[dt.datetime]
            Upper bound (inclusive) of the time range. Defaults to now.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IssueAnalyticsResponse
            Issue analytics

        Examples
        --------
        import asyncio
        import datetime

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.issues.analytics(
                project_slug="projectSlug",
                from_iso=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
                to_iso=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.analytics(
            project_slug, from_iso=from_iso, to_iso=to_iso, request_options=request_options
        )
        return _response.data

    async def get(
        self, project_slug: str, issue_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> IssueDetail:
        """
        Returns the full-history detail view of one issue: lifecycle `states`, lifetime activity stats (`firstSeenAt`, `lastSeenAt`, `occurrences`, `affectedTracesPercent`, `tags`), a 14-day occurrence `trend`, the active `evaluations` monitoring it, and the current `monitoringState`.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_slug : str
            Issue slug.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IssueDetail
            Issue

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.issues.get(
                project_slug="projectSlug",
                issue_slug="issueSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(project_slug, issue_slug, request_options=request_options)
        return _response.data

    async def trend(
        self,
        project_slug: str,
        issue_slug: str,
        *,
        from_iso: typing.Optional[dt.datetime] = None,
        to_iso: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IssueHistogram:
        """
        Returns the occurrence histogram for one issue over `[fromIso, toIso]`. The default range is the trailing 14 days. Buckets are 12-hour wide and UTC-aligned.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_slug : str
            Issue slug.

        from_iso : typing.Optional[dt.datetime]
            Lower bound (inclusive). Defaults to ~14 days before `toIso`.

        to_iso : typing.Optional[dt.datetime]
            Upper bound (inclusive). Defaults to now.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IssueHistogram
            Occurrence histogram

        Examples
        --------
        import asyncio
        import datetime

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.issues.trend(
                project_slug="projectSlug",
                issue_slug="issueSlug",
                from_iso=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
                to_iso=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.trend(
            project_slug, issue_slug, from_iso=from_iso, to_iso=to_iso, request_options=request_options
        )
        return _response.data

    async def list_traces(
        self,
        project_slug: str,
        issue_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedTraces:
        """
        Returns the page of distinct traces that contributed at least one occurrence of the issue, ordered by most recent activity first.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_slug : str
            Issue slug.

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedTraces
            Page of traces

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.issues.list_traces(
                project_slug="projectSlug",
                issue_slug="issueSlug",
                cursor="cursor",
                limit=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_traces(
            project_slug, issue_slug, cursor=cursor, limit=limit, request_options=request_options
        )
        return _response.data

    async def resolve(
        self,
        project_slug: str,
        *,
        issue_ids: typing.Sequence[str],
        keep_monitoring: typing.Optional[bool] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IssuesLifecycleResponse:
        """
        Marks each issue in `issueIds` as resolved. When `keepMonitoring` is `false`, monitoring is also stopped for each resolved issue; when omitted, the project's default applies.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_ids : typing.Sequence[str]
            Non-empty list of issue ids. Operations are idempotent — already-applied issues are unchanged.

        keep_monitoring : typing.Optional[bool]
            When `true`, monitoring continues after the issues are resolved. When `false`, monitoring stops. Defaults to the project setting.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IssuesLifecycleResponse
            Per-issue result

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.issues.resolve(
                project_slug="projectSlug",
                issue_ids=["issueIds"],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.resolve(
            project_slug, issue_ids=issue_ids, keep_monitoring=keep_monitoring, request_options=request_options
        )
        return _response.data

    async def unresolve(
        self,
        project_slug: str,
        *,
        issue_ids: typing.Sequence[str],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IssuesLifecycleResponse:
        """
        Reverts each issue in `issueIds` to the unresolved state.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_ids : typing.Sequence[str]
            Non-empty list of issue ids. Operations are idempotent — already-applied issues are unchanged.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IssuesLifecycleResponse
            Per-issue result

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.issues.unresolve(
                project_slug="projectSlug",
                issue_ids=["issueIds"],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.unresolve(project_slug, issue_ids=issue_ids, request_options=request_options)
        return _response.data

    async def ignore(
        self,
        project_slug: str,
        *,
        issue_ids: typing.Sequence[str],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IssuesLifecycleResponse:
        """
        Marks each issue in `issueIds` as ignored. Monitoring is also stopped for each ignored issue.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_ids : typing.Sequence[str]
            Non-empty list of issue ids. Operations are idempotent — already-applied issues are unchanged.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IssuesLifecycleResponse
            Per-issue result

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.issues.ignore(
                project_slug="projectSlug",
                issue_ids=["issueIds"],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.ignore(project_slug, issue_ids=issue_ids, request_options=request_options)
        return _response.data

    async def unignore(
        self,
        project_slug: str,
        *,
        issue_ids: typing.Sequence[str],
        request_options: typing.Optional[RequestOptions] = None,
    ) -> IssuesLifecycleResponse:
        """
        Reverts each issue in `issueIds` to a non-ignored state.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_ids : typing.Sequence[str]
            Non-empty list of issue ids. Operations are idempotent — already-applied issues are unchanged.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        IssuesLifecycleResponse
            Per-issue result

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.issues.unignore(
                project_slug="projectSlug",
                issue_ids=["issueIds"],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.unignore(project_slug, issue_ids=issue_ids, request_options=request_options)
        return _response.data

    async def monitor(
        self, project_slug: str, issue_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> MonitorIssueResponse:
        """
        Starts (or realigns) monitoring for the issue. When the issue has no active evaluation, a new one is generated. When an active evaluation exists, the call realigns it. The work runs asynchronously and the response returns immediately. Returns 400 when monitoring is already in progress for this issue.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_slug : str
            Issue slug.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MonitorIssueResponse
            Monitor job enqueued

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.issues.monitor(
                project_slug="projectSlug",
                issue_slug="issueSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.monitor(project_slug, issue_slug, request_options=request_options)
        return _response.data

    async def unmonitor(
        self, project_slug: str, issue_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Stops monitoring the issue. Idempotent — issues that aren't being monitored return 204 without changing anything.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        issue_slug : str
            Issue slug.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.issues.unmonitor(
                project_slug="projectSlug",
                issue_slug="issueSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.unmonitor(project_slug, issue_slug, request_options=request_options)
        return _response.data

    async def export(
        self,
        project_slug: str,
        *,
        recipient: str,
        issue_ids: typing.Optional[typing.Sequence[str]] = OMIT,
        lifecycle_group: typing.Optional[ExportIssuesBodyLifecycleGroup] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ExportIssuesResponse:
        """
        Enqueues an asynchronous CSV export. The response returns immediately; the download link is emailed to `recipient` when the file is ready. The recipient must be a member of the requesting organization.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        recipient : str
            Email address the download link is sent to. Must belong to a member of the requesting organization.

        issue_ids : typing.Optional[typing.Sequence[str]]
            Restrict the export to this subset of issues. Omit to export every issue in the project.

        lifecycle_group : typing.Optional[ExportIssuesBodyLifecycleGroup]
            `"active"` for unresolved/unignored issues; `"archived"` for the rest. Omit to include both.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ExportIssuesResponse
            Export enqueued

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.issues.export(
                project_slug="projectSlug",
                recipient="recipient",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.export(
            project_slug,
            recipient=recipient,
            issue_ids=issue_ids,
            lifecycle_group=lifecycle_group,
            request_options=request_options,
        )
        return _response.data
