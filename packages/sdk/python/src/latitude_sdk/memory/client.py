# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.memory_record_change_diff import MemoryRecordChangeDiff
from ..types.memory_record_detail import MemoryRecordDetail
from ..types.memory_record_reads import MemoryRecordReads
from ..types.memory_record_users import MemoryRecordUsers
from ..types.memory_store_diff import MemoryStoreDiff
from ..types.memory_store_snapshot import MemoryStoreSnapshot
from ..types.memory_store_users import MemoryStoreUsers
from ..types.paginated_memory_stores import PaginatedMemoryStores
from .raw_client import AsyncRawMemoryClient, RawMemoryClient
from .types.list_stores_memory_request_direction import ListStoresMemoryRequestDirection
from .types.list_stores_memory_request_sort import ListStoresMemoryRequestSort


class MemoryClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawMemoryClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawMemoryClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawMemoryClient
        """
        return self._raw_client

    def list_stores(
        self,
        project_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        sort: typing.Optional[ListStoresMemoryRequestSort] = None,
        direction: typing.Optional[ListStoresMemoryRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedMemoryStores:
        """
        Returns a cursor-paginated page of the project's memory stores, one roll-up row each (record count, tokens, last-updated, sessions, users). A store groups records under `gen_ai.memory.store.id`; the empty-string store is the unattributed bucket.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        sort : typing.Optional[ListStoresMemoryRequestSort]
            Field to sort by. Defaults to `lastUpdated` (most recently written first).

        direction : typing.Optional[ListStoresMemoryRequestDirection]
            Sort direction. Defaults to `desc`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedMemoryStores
            Page of memory stores

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.memory.list_stores(
            project_slug="projectSlug",
        )
        """
        _response = self._raw_client.list_stores(
            project_slug, cursor=cursor, limit=limit, sort=sort, direction=direction, request_options=request_options
        )
        return _response.data

    def get_store(
        self,
        project_slug: str,
        *,
        store_id: str,
        at: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MemoryStoreSnapshot:
        """
        Returns the store's current records (ids, token counts, last-updated) as a snapshot. Pass `at` (ISO-8601) to reconstruct the store as of a past point in time. Record bodies are fetched separately, one record at a time.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        store_id : str
            Store identifier (`gen_ai.memory.store.id`). Pass an empty string to address the unattributed ("") store.

        at : typing.Optional[dt.datetime]
            Reconstruct the store as of this ISO-8601 timestamp. Defaults to the current state.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MemoryStoreSnapshot
            Memory store snapshot

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.memory.get_store(
            project_slug="projectSlug",
            store_id="storeId",
        )
        """
        _response = self._raw_client.get_store(project_slug, store_id=store_id, at=at, request_options=request_options)
        return _response.data

    def get_store_diff(
        self,
        project_slug: str,
        *,
        store_id: str,
        from_: typing.Optional[dt.datetime] = None,
        to: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MemoryStoreDiff:
        """
        Returns a per-record diff of the store between two points in time — added, updated, and removed records with token deltas. `from` defaults to the empty state (everything counts as added); `to` defaults to the current state. Unchanged records are pruned.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        store_id : str
            Store identifier (`gen_ai.memory.store.id`). Pass an empty string to address the unattributed ("") store.

        from_ : typing.Optional[dt.datetime]
            Lower bound (inclusive) of the diff, ISO-8601. Defaults to the empty state.

        to : typing.Optional[dt.datetime]
            Upper bound (inclusive) of the diff, ISO-8601. Defaults to the current state.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MemoryStoreDiff
            Memory store diff

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.memory.get_store_diff(
            project_slug="projectSlug",
            store_id="storeId",
        )
        """
        _response = self._raw_client.get_store_diff(
            project_slug, store_id=store_id, from_=from_, to=to, request_options=request_options
        )
        return _response.data

    def list_store_users(
        self, project_slug: str, *, store_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> MemoryStoreUsers:
        """
        Returns the end-users who accessed the store (reads and writes both count as access), most recent access first. Capped at the 1000 most recent accessors.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        store_id : str
            Store identifier (`gen_ai.memory.store.id`). Pass an empty string to address the unattributed ("") store.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MemoryStoreUsers
            Users of the store

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.memory.list_store_users(
            project_slug="projectSlug",
            store_id="storeId",
        )
        """
        _response = self._raw_client.list_store_users(project_slug, store_id=store_id, request_options=request_options)
        return _response.data

    def get_record(
        self,
        project_slug: str,
        *,
        store_id: str,
        record_id: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MemoryRecordDetail:
        """
        Returns one record's current body plus its mutating version history (newest first), each version carrying the authoring span/trace/session/user and per-version token deltas.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        store_id : str
            Store identifier (`gen_ai.memory.store.id`). Pass an empty string to address the unattributed ("") store.

        record_id : str
            Record identifier (`gen_ai.memory.record.id`). Pass an empty string to address the unnamed record.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MemoryRecordDetail
            Memory record detail

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.memory.get_record(
            project_slug="projectSlug",
            store_id="storeId",
            record_id="recordId",
        )
        """
        _response = self._raw_client.get_record(
            project_slug, store_id=store_id, record_id=record_id, request_options=request_options
        )
        return _response.data

    def get_record_change(
        self,
        project_slug: str,
        *,
        store_id: str,
        record_id: str,
        span_id: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MemoryRecordChangeDiff:
        """
        Returns the before/after bodies for one change — the version authored by `spanId` against its predecessor in the record's mutating chain. Returns 404 when the span is not a recorded change of the record.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        store_id : str
            Store identifier (`gen_ai.memory.store.id`). Pass an empty string to address the unattributed ("") store.

        record_id : str
            Record identifier (`gen_ai.memory.record.id`). Pass an empty string to address the unnamed record.

        span_id : str
            Span that authored the change (the `after` side).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MemoryRecordChangeDiff
            Memory record change diff

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.memory.get_record_change(
            project_slug="projectSlug",
            store_id="storeId",
            record_id="recordId",
            span_id="spanId",
        )
        """
        _response = self._raw_client.get_record_change(
            project_slug, store_id=store_id, record_id=record_id, span_id=span_id, request_options=request_options
        )
        return _response.data

    def list_record_reads(
        self,
        project_slug: str,
        *,
        store_id: str,
        record_id: str,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MemoryRecordReads:
        """
        Returns the retrieval (`search_memory`) events for one record, newest first and capped, each with the query text (when captured), tokens returned, and the accessing span/trace/session/user.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        store_id : str
            Store identifier (`gen_ai.memory.store.id`). Pass an empty string to address the unattributed ("") store.

        record_id : str
            Record identifier (`gen_ai.memory.record.id`). Pass an empty string to address the unnamed record.

        limit : typing.Optional[int]
            Maximum number of read events to return. Capped at 200.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MemoryRecordReads
            Reads of the record

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.memory.list_record_reads(
            project_slug="projectSlug",
            store_id="storeId",
            record_id="recordId",
        )
        """
        _response = self._raw_client.list_record_reads(
            project_slug, store_id=store_id, record_id=record_id, limit=limit, request_options=request_options
        )
        return _response.data

    def list_record_users(
        self,
        project_slug: str,
        *,
        store_id: str,
        record_id: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MemoryRecordUsers:
        """
        Returns the end-users who accessed one record with per-user read and write counts, most recent access first. Capped at the 1000 most recent accessors.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        store_id : str
            Store identifier (`gen_ai.memory.store.id`). Pass an empty string to address the unattributed ("") store.

        record_id : str
            Record identifier (`gen_ai.memory.record.id`). Pass an empty string to address the unnamed record.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MemoryRecordUsers
            Users of the record

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.memory.list_record_users(
            project_slug="projectSlug",
            store_id="storeId",
            record_id="recordId",
        )
        """
        _response = self._raw_client.list_record_users(
            project_slug, store_id=store_id, record_id=record_id, request_options=request_options
        )
        return _response.data


class AsyncMemoryClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawMemoryClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawMemoryClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawMemoryClient
        """
        return self._raw_client

    async def list_stores(
        self,
        project_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        sort: typing.Optional[ListStoresMemoryRequestSort] = None,
        direction: typing.Optional[ListStoresMemoryRequestDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedMemoryStores:
        """
        Returns a cursor-paginated page of the project's memory stores, one roll-up row each (record count, tokens, last-updated, sessions, users). A store groups records under `gen_ai.memory.store.id`; the empty-string store is the unattributed bucket.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        sort : typing.Optional[ListStoresMemoryRequestSort]
            Field to sort by. Defaults to `lastUpdated` (most recently written first).

        direction : typing.Optional[ListStoresMemoryRequestDirection]
            Sort direction. Defaults to `desc`.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedMemoryStores
            Page of memory stores

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.memory.list_stores(
                project_slug="projectSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_stores(
            project_slug, cursor=cursor, limit=limit, sort=sort, direction=direction, request_options=request_options
        )
        return _response.data

    async def get_store(
        self,
        project_slug: str,
        *,
        store_id: str,
        at: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MemoryStoreSnapshot:
        """
        Returns the store's current records (ids, token counts, last-updated) as a snapshot. Pass `at` (ISO-8601) to reconstruct the store as of a past point in time. Record bodies are fetched separately, one record at a time.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        store_id : str
            Store identifier (`gen_ai.memory.store.id`). Pass an empty string to address the unattributed ("") store.

        at : typing.Optional[dt.datetime]
            Reconstruct the store as of this ISO-8601 timestamp. Defaults to the current state.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MemoryStoreSnapshot
            Memory store snapshot

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.memory.get_store(
                project_slug="projectSlug",
                store_id="storeId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_store(
            project_slug, store_id=store_id, at=at, request_options=request_options
        )
        return _response.data

    async def get_store_diff(
        self,
        project_slug: str,
        *,
        store_id: str,
        from_: typing.Optional[dt.datetime] = None,
        to: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MemoryStoreDiff:
        """
        Returns a per-record diff of the store between two points in time — added, updated, and removed records with token deltas. `from` defaults to the empty state (everything counts as added); `to` defaults to the current state. Unchanged records are pruned.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        store_id : str
            Store identifier (`gen_ai.memory.store.id`). Pass an empty string to address the unattributed ("") store.

        from_ : typing.Optional[dt.datetime]
            Lower bound (inclusive) of the diff, ISO-8601. Defaults to the empty state.

        to : typing.Optional[dt.datetime]
            Upper bound (inclusive) of the diff, ISO-8601. Defaults to the current state.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MemoryStoreDiff
            Memory store diff

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.memory.get_store_diff(
                project_slug="projectSlug",
                store_id="storeId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_store_diff(
            project_slug, store_id=store_id, from_=from_, to=to, request_options=request_options
        )
        return _response.data

    async def list_store_users(
        self, project_slug: str, *, store_id: str, request_options: typing.Optional[RequestOptions] = None
    ) -> MemoryStoreUsers:
        """
        Returns the end-users who accessed the store (reads and writes both count as access), most recent access first. Capped at the 1000 most recent accessors.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        store_id : str
            Store identifier (`gen_ai.memory.store.id`). Pass an empty string to address the unattributed ("") store.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MemoryStoreUsers
            Users of the store

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.memory.list_store_users(
                project_slug="projectSlug",
                store_id="storeId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_store_users(
            project_slug, store_id=store_id, request_options=request_options
        )
        return _response.data

    async def get_record(
        self,
        project_slug: str,
        *,
        store_id: str,
        record_id: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MemoryRecordDetail:
        """
        Returns one record's current body plus its mutating version history (newest first), each version carrying the authoring span/trace/session/user and per-version token deltas.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        store_id : str
            Store identifier (`gen_ai.memory.store.id`). Pass an empty string to address the unattributed ("") store.

        record_id : str
            Record identifier (`gen_ai.memory.record.id`). Pass an empty string to address the unnamed record.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MemoryRecordDetail
            Memory record detail

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.memory.get_record(
                project_slug="projectSlug",
                store_id="storeId",
                record_id="recordId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_record(
            project_slug, store_id=store_id, record_id=record_id, request_options=request_options
        )
        return _response.data

    async def get_record_change(
        self,
        project_slug: str,
        *,
        store_id: str,
        record_id: str,
        span_id: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MemoryRecordChangeDiff:
        """
        Returns the before/after bodies for one change — the version authored by `spanId` against its predecessor in the record's mutating chain. Returns 404 when the span is not a recorded change of the record.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        store_id : str
            Store identifier (`gen_ai.memory.store.id`). Pass an empty string to address the unattributed ("") store.

        record_id : str
            Record identifier (`gen_ai.memory.record.id`). Pass an empty string to address the unnamed record.

        span_id : str
            Span that authored the change (the `after` side).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MemoryRecordChangeDiff
            Memory record change diff

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.memory.get_record_change(
                project_slug="projectSlug",
                store_id="storeId",
                record_id="recordId",
                span_id="spanId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_record_change(
            project_slug, store_id=store_id, record_id=record_id, span_id=span_id, request_options=request_options
        )
        return _response.data

    async def list_record_reads(
        self,
        project_slug: str,
        *,
        store_id: str,
        record_id: str,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MemoryRecordReads:
        """
        Returns the retrieval (`search_memory`) events for one record, newest first and capped, each with the query text (when captured), tokens returned, and the accessing span/trace/session/user.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        store_id : str
            Store identifier (`gen_ai.memory.store.id`). Pass an empty string to address the unattributed ("") store.

        record_id : str
            Record identifier (`gen_ai.memory.record.id`). Pass an empty string to address the unnamed record.

        limit : typing.Optional[int]
            Maximum number of read events to return. Capped at 200.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MemoryRecordReads
            Reads of the record

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.memory.list_record_reads(
                project_slug="projectSlug",
                store_id="storeId",
                record_id="recordId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_record_reads(
            project_slug, store_id=store_id, record_id=record_id, limit=limit, request_options=request_options
        )
        return _response.data

    async def list_record_users(
        self,
        project_slug: str,
        *,
        store_id: str,
        record_id: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MemoryRecordUsers:
        """
        Returns the end-users who accessed one record with per-user read and write counts, most recent access first. Capped at the 1000 most recent accessors.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        store_id : str
            Store identifier (`gen_ai.memory.store.id`). Pass an empty string to address the unattributed ("") store.

        record_id : str
            Record identifier (`gen_ai.memory.record.id`). Pass an empty string to address the unnamed record.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MemoryRecordUsers
            Users of the record

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.memory.list_record_users(
                project_slug="projectSlug",
                store_id="storeId",
                record_id="recordId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_record_users(
            project_slug, store_id=store_id, record_id=record_id, request_options=request_options
        )
        return _response.data
