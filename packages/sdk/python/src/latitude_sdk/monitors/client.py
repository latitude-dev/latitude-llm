# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.create_monitor_alert_body import CreateMonitorAlertBody
from ..types.monitor import Monitor
from ..types.monitor_alert import MonitorAlert
from ..types.monitor_alert_list import MonitorAlertList
from ..types.paginated_monitor_incidents import PaginatedMonitorIncidents
from ..types.paginated_monitors import PaginatedMonitors
from .raw_client import AsyncRawMonitorsClient, RawMonitorsClient
from .types.update_monitor_alert_body_condition import UpdateMonitorAlertBodyCondition
from .types.update_monitor_alert_body_kind import UpdateMonitorAlertBodyKind
from .types.update_monitor_alert_body_severity import UpdateMonitorAlertBodySeverity
from .types.update_monitor_alert_body_source import UpdateMonitorAlertBodySource

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class MonitorsClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawMonitorsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawMonitorsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawMonitorsClient
        """
        return self._raw_client

    def list(
        self,
        project_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedMonitors:
        """
        Returns the project's monitors, system monitors first, then by most recent activity.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 100.

        search : typing.Optional[str]
            Filter by name (case-insensitive substring).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedMonitors
            Page of monitors

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.monitors.list(
            project_slug="projectSlug",
            cursor="cursor",
            limit=1,
            search="search",
        )
        """
        _response = self._raw_client.list(
            project_slug, cursor=cursor, limit=limit, search=search, request_options=request_options
        )
        return _response.data

    def create(
        self,
        project_slug: str,
        *,
        name: str,
        alerts: typing.Sequence[CreateMonitorAlertBody],
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Monitor:
        """
        Creates a monitor with its saved-search alert. The slug is derived from `name`. The watched saved search must not contain a semantic component (unquoted free text) — monitors need an exact match rule, so only quoted literal and backtick phrase terms are allowed.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        name : str
            Human-readable name. Used to derive the slug.

        alerts : typing.Sequence[CreateMonitorAlertBody]
            The monitor's alert. Exactly one.

        description : typing.Optional[str]
            Optional free-form description.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Monitor
            Monitor created

        Examples
        --------
        from latitude import (
            CreateMonitorAlertBody,
            CreateMonitorAlertBodySource,
            LatitudeApiClient,
        )

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.monitors.create(
            project_slug="projectSlug",
            name="name",
            alerts=[
                CreateMonitorAlertBody(
                    kind="savedSearch.match",
                    source=CreateMonitorAlertBodySource(
                        type="issue",
                        id="id",
                    ),
                )
            ],
        )
        """
        _response = self._raw_client.create(
            project_slug, name=name, alerts=alerts, description=description, request_options=request_options
        )
        return _response.data

    def get(
        self, project_slug: str, monitor_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> Monitor:
        """
        Returns a single monitor by slug.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Monitor
            Monitor

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.monitors.get(
            project_slug="projectSlug",
            monitor_slug="monitorSlug",
        )
        """
        _response = self._raw_client.get(project_slug, monitor_slug, request_options=request_options)
        return _response.data

    def delete(
        self, project_slug: str, monitor_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Deletes a monitor and its alerts. System monitors cannot be deleted.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.monitors.delete(
            project_slug="projectSlug",
            monitor_slug="monitorSlug",
        )
        """
        _response = self._raw_client.delete(project_slug, monitor_slug, request_options=request_options)
        return _response.data

    def update(
        self,
        project_slug: str,
        monitor_slug: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Monitor:
        """
        Updates a monitor's name and description. System monitors cannot be edited.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        name : typing.Optional[str]
            New name. Renaming may regenerate the slug — re-read the response or rely on `id`.

        description : typing.Optional[str]
            New description.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Monitor
            Updated monitor

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.monitors.update(
            project_slug="projectSlug",
            monitor_slug="monitorSlug",
        )
        """
        _response = self._raw_client.update(
            project_slug, monitor_slug, name=name, description=description, request_options=request_options
        )
        return _response.data

    def list_alerts(
        self, project_slug: str, monitor_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> MonitorAlertList:
        """
        Returns the monitor's alerts.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MonitorAlertList
            The monitor's alerts

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.monitors.list_alerts(
            project_slug="projectSlug",
            monitor_slug="monitorSlug",
        )
        """
        _response = self._raw_client.list_alerts(project_slug, monitor_slug, request_options=request_options)
        return _response.data

    def get_alert(
        self,
        project_slug: str,
        monitor_slug: str,
        alert_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MonitorAlert:
        """
        Returns a single monitor alert by id.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        alert_id : str
            Monitor-alert identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MonitorAlert
            Monitor alert

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.monitors.get_alert(
            project_slug="projectSlug",
            monitor_slug="monitorSlug",
            alert_id="alertId",
        )
        """
        _response = self._raw_client.get_alert(project_slug, monitor_slug, alert_id, request_options=request_options)
        return _response.data

    def update_alert(
        self,
        project_slug: str,
        monitor_slug: str,
        alert_id: str,
        *,
        kind: typing.Optional[UpdateMonitorAlertBodyKind] = OMIT,
        source: typing.Optional[UpdateMonitorAlertBodySource] = OMIT,
        condition: typing.Optional[UpdateMonitorAlertBodyCondition] = OMIT,
        severity: typing.Optional[UpdateMonitorAlertBodySeverity] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Monitor:
        """
        Updates an alert and returns the updated monitor. On system monitors only the condition may change; on your own monitors any field may. Re-pointing the alert at a saved search containing a semantic component (unquoted free text) is rejected.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        alert_id : str
            Monitor-alert identifier.

        kind : typing.Optional[UpdateMonitorAlertBodyKind]
            New alert kind. Not allowed on system monitors. Supply the matching `condition` when you change it.

        source : typing.Optional[UpdateMonitorAlertBodySource]
            Replace the watched saved search. Not allowed on system monitors.

        condition : typing.Optional[UpdateMonitorAlertBodyCondition]
            Replace the alert's configuration. On system monitors this is the only editable field (e.g. signal-escalation `sensitivity`).

        severity : typing.Optional[UpdateMonitorAlertBodySeverity]
            Replace the severity. Not allowed on system monitors.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Monitor
            Monitor with the updated alert

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.monitors.update_alert(
            project_slug="projectSlug",
            monitor_slug="monitorSlug",
            alert_id="alertId",
        )
        """
        _response = self._raw_client.update_alert(
            project_slug,
            monitor_slug,
            alert_id,
            kind=kind,
            source=source,
            condition=condition,
            severity=severity,
            request_options=request_options,
        )
        return _response.data

    def list_incidents(
        self,
        project_slug: str,
        monitor_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedMonitorIncidents:
        """
        Returns the incidents opened by a monitor, most recent first. Each item's `notified` flag shows whether it triggered a notification.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 100.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedMonitorIncidents
            Page of incidents

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.monitors.list_incidents(
            project_slug="projectSlug",
            monitor_slug="monitorSlug",
            cursor="cursor",
            limit=1,
        )
        """
        _response = self._raw_client.list_incidents(
            project_slug, monitor_slug, cursor=cursor, limit=limit, request_options=request_options
        )
        return _response.data

    def mute(
        self, project_slug: str, monitor_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> Monitor:
        """
        Mutes a monitor so its incidents stop sending notifications. Allowed on all monitors.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Monitor
            Muted monitor

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.monitors.mute(
            project_slug="projectSlug",
            monitor_slug="monitorSlug",
        )
        """
        _response = self._raw_client.mute(project_slug, monitor_slug, request_options=request_options)
        return _response.data

    def unmute(
        self, project_slug: str, monitor_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> Monitor:
        """
        Lifts a monitor's mute so its incidents notify again.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Monitor
            Unmuted monitor

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.monitors.unmute(
            project_slug="projectSlug",
            monitor_slug="monitorSlug",
        )
        """
        _response = self._raw_client.unmute(project_slug, monitor_slug, request_options=request_options)
        return _response.data


class AsyncMonitorsClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawMonitorsClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawMonitorsClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawMonitorsClient
        """
        return self._raw_client

    async def list(
        self,
        project_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        search: typing.Optional[str] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedMonitors:
        """
        Returns the project's monitors, system monitors first, then by most recent activity.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 100.

        search : typing.Optional[str]
            Filter by name (case-insensitive substring).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedMonitors
            Page of monitors

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.monitors.list(
                project_slug="projectSlug",
                cursor="cursor",
                limit=1,
                search="search",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            project_slug, cursor=cursor, limit=limit, search=search, request_options=request_options
        )
        return _response.data

    async def create(
        self,
        project_slug: str,
        *,
        name: str,
        alerts: typing.Sequence[CreateMonitorAlertBody],
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Monitor:
        """
        Creates a monitor with its saved-search alert. The slug is derived from `name`. The watched saved search must not contain a semantic component (unquoted free text) — monitors need an exact match rule, so only quoted literal and backtick phrase terms are allowed.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        name : str
            Human-readable name. Used to derive the slug.

        alerts : typing.Sequence[CreateMonitorAlertBody]
            The monitor's alert. Exactly one.

        description : typing.Optional[str]
            Optional free-form description.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Monitor
            Monitor created

        Examples
        --------
        import asyncio

        from latitude import (
            AsyncLatitudeApiClient,
            CreateMonitorAlertBody,
            CreateMonitorAlertBodySource,
        )

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.monitors.create(
                project_slug="projectSlug",
                name="name",
                alerts=[
                    CreateMonitorAlertBody(
                        kind="savedSearch.match",
                        source=CreateMonitorAlertBodySource(
                            type="issue",
                            id="id",
                        ),
                    )
                ],
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            project_slug, name=name, alerts=alerts, description=description, request_options=request_options
        )
        return _response.data

    async def get(
        self, project_slug: str, monitor_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> Monitor:
        """
        Returns a single monitor by slug.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Monitor
            Monitor

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.monitors.get(
                project_slug="projectSlug",
                monitor_slug="monitorSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(project_slug, monitor_slug, request_options=request_options)
        return _response.data

    async def delete(
        self, project_slug: str, monitor_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Deletes a monitor and its alerts. System monitors cannot be deleted.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.monitors.delete(
                project_slug="projectSlug",
                monitor_slug="monitorSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(project_slug, monitor_slug, request_options=request_options)
        return _response.data

    async def update(
        self,
        project_slug: str,
        monitor_slug: str,
        *,
        name: typing.Optional[str] = OMIT,
        description: typing.Optional[str] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Monitor:
        """
        Updates a monitor's name and description. System monitors cannot be edited.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        name : typing.Optional[str]
            New name. Renaming may regenerate the slug — re-read the response or rely on `id`.

        description : typing.Optional[str]
            New description.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Monitor
            Updated monitor

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.monitors.update(
                project_slug="projectSlug",
                monitor_slug="monitorSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            project_slug, monitor_slug, name=name, description=description, request_options=request_options
        )
        return _response.data

    async def list_alerts(
        self, project_slug: str, monitor_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> MonitorAlertList:
        """
        Returns the monitor's alerts.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MonitorAlertList
            The monitor's alerts

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.monitors.list_alerts(
                project_slug="projectSlug",
                monitor_slug="monitorSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_alerts(project_slug, monitor_slug, request_options=request_options)
        return _response.data

    async def get_alert(
        self,
        project_slug: str,
        monitor_slug: str,
        alert_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> MonitorAlert:
        """
        Returns a single monitor alert by id.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        alert_id : str
            Monitor-alert identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        MonitorAlert
            Monitor alert

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.monitors.get_alert(
                project_slug="projectSlug",
                monitor_slug="monitorSlug",
                alert_id="alertId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_alert(
            project_slug, monitor_slug, alert_id, request_options=request_options
        )
        return _response.data

    async def update_alert(
        self,
        project_slug: str,
        monitor_slug: str,
        alert_id: str,
        *,
        kind: typing.Optional[UpdateMonitorAlertBodyKind] = OMIT,
        source: typing.Optional[UpdateMonitorAlertBodySource] = OMIT,
        condition: typing.Optional[UpdateMonitorAlertBodyCondition] = OMIT,
        severity: typing.Optional[UpdateMonitorAlertBodySeverity] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Monitor:
        """
        Updates an alert and returns the updated monitor. On system monitors only the condition may change; on your own monitors any field may. Re-pointing the alert at a saved search containing a semantic component (unquoted free text) is rejected.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        alert_id : str
            Monitor-alert identifier.

        kind : typing.Optional[UpdateMonitorAlertBodyKind]
            New alert kind. Not allowed on system monitors. Supply the matching `condition` when you change it.

        source : typing.Optional[UpdateMonitorAlertBodySource]
            Replace the watched saved search. Not allowed on system monitors.

        condition : typing.Optional[UpdateMonitorAlertBodyCondition]
            Replace the alert's configuration. On system monitors this is the only editable field (e.g. signal-escalation `sensitivity`).

        severity : typing.Optional[UpdateMonitorAlertBodySeverity]
            Replace the severity. Not allowed on system monitors.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Monitor
            Monitor with the updated alert

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.monitors.update_alert(
                project_slug="projectSlug",
                monitor_slug="monitorSlug",
                alert_id="alertId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update_alert(
            project_slug,
            monitor_slug,
            alert_id,
            kind=kind,
            source=source,
            condition=condition,
            severity=severity,
            request_options=request_options,
        )
        return _response.data

    async def list_incidents(
        self,
        project_slug: str,
        monitor_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedMonitorIncidents:
        """
        Returns the incidents opened by a monitor, most recent first. Each item's `notified` flag shows whether it triggered a notification.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 100.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedMonitorIncidents
            Page of incidents

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.monitors.list_incidents(
                project_slug="projectSlug",
                monitor_slug="monitorSlug",
                cursor="cursor",
                limit=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_incidents(
            project_slug, monitor_slug, cursor=cursor, limit=limit, request_options=request_options
        )
        return _response.data

    async def mute(
        self, project_slug: str, monitor_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> Monitor:
        """
        Mutes a monitor so its incidents stop sending notifications. Allowed on all monitors.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Monitor
            Muted monitor

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.monitors.mute(
                project_slug="projectSlug",
                monitor_slug="monitorSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.mute(project_slug, monitor_slug, request_options=request_options)
        return _response.data

    async def unmute(
        self, project_slug: str, monitor_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> Monitor:
        """
        Lifts a monitor's mute so its incidents notify again.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        monitor_slug : str
            Monitor slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Monitor
            Unmuted monitor

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.monitors.unmute(
                project_slug="projectSlug",
                monitor_slug="monitorSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.unmute(project_slug, monitor_slug, request_options=request_options)
        return _response.data
