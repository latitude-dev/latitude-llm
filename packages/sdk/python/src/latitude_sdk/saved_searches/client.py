# This file was auto-generated by Fern from our API Definition.

import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.filter_condition import FilterCondition
from ..types.paginated_saved_searches import PaginatedSavedSearches
from ..types.paginated_traces import PaginatedTraces
from ..types.saved_search import SavedSearch
from .raw_client import AsyncRawSavedSearchesClient, RawSavedSearchesClient
from .types.list_traces_saved_searches_request_sort_by import ListTracesSavedSearchesRequestSortBy
from .types.list_traces_saved_searches_request_sort_direction import ListTracesSavedSearchesRequestSortDirection

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class SavedSearchesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawSavedSearchesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawSavedSearchesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawSavedSearchesClient
        """
        return self._raw_client

    def list(
        self, project_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PaginatedSavedSearches:
        """
        Returns every saved search in the project. The response uses the standard paginated shape; the saved-search list currently fits in a single page (`nextCursor` is always `null`).

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedSavedSearches
            List of saved searches

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.saved_searches.list(
            project_slug="projectSlug",
        )
        """
        _response = self._raw_client.list(project_slug, request_options=request_options)
        return _response.data

    def create(
        self,
        project_slug: str,
        *,
        name: str,
        query: typing.Optional[str] = OMIT,
        filters: typing.Optional[typing.Dict[str, typing.Sequence[FilterCondition]]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSearch:
        """
        Creates a saved search within the project. At least one of `query` or `filters` must be set. The slug is derived from `name`. OAuth-authenticated only.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        name : str
            Human-readable name. Used to derive the slug.

        query : typing.Optional[str]
            Free-text semantic query. `null` (default) when the search is filter-only. At least one of `query` or `filters` must be set.

        filters : typing.Optional[typing.Dict[str, typing.Sequence[FilterCondition]]]
            Structured filter set. Defaults to `{}` (no filters). At least one of `query` or `filters` must be set.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSearch
            Saved search created

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.saved_searches.create(
            project_slug="projectSlug",
            name="name",
        )
        """
        _response = self._raw_client.create(
            project_slug, name=name, query=query, filters=filters, request_options=request_options
        )
        return _response.data

    def get(
        self, project_slug: str, search_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SavedSearch:
        """
        Returns a single saved search by slug.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        search_slug : str
            Saved-search slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSearch
            Saved search

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.saved_searches.get(
            project_slug="projectSlug",
            search_slug="searchSlug",
        )
        """
        _response = self._raw_client.get(project_slug, search_slug, request_options=request_options)
        return _response.data

    def delete(
        self, project_slug: str, search_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Deletes a saved search by slug.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        search_slug : str
            Saved-search slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.saved_searches.delete(
            project_slug="projectSlug",
            search_slug="searchSlug",
        )
        """
        _response = self._raw_client.delete(project_slug, search_slug, request_options=request_options)
        return _response.data

    def update(
        self,
        project_slug: str,
        search_slug: str,
        *,
        name: typing.Optional[str] = OMIT,
        query: typing.Optional[str] = OMIT,
        filters: typing.Optional[typing.Dict[str, typing.Sequence[FilterCondition]]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSearch:
        """
        Updates a saved search. Renaming may regenerate the slug — clients should re-read the response or rely on the `id` for stable references.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        search_slug : str
            Saved-search slug (human-readable identifier within the project).

        name : typing.Optional[str]
            New human-readable name. Triggers slug regeneration when the change affects the slug form (cosmetic edits like capitalization keep the URL stable).

        query : typing.Optional[str]
            Replace the free-text query. Pass `null` to clear it.

        filters : typing.Optional[typing.Dict[str, typing.Sequence[FilterCondition]]]
            Replace the structured filter set.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSearch
            Updated saved search

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.saved_searches.update(
            project_slug="projectSlug",
            search_slug="searchSlug",
        )
        """
        _response = self._raw_client.update(
            project_slug, search_slug, name=name, query=query, filters=filters, request_options=request_options
        )
        return _response.data

    def list_traces(
        self,
        project_slug: str,
        search_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        sort_by: typing.Optional[ListTracesSavedSearchesRequestSortBy] = None,
        sort_direction: typing.Optional[ListTracesSavedSearchesRequestSortDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedTraces:
        """
        Returns a cursor-paginated page of traces that match the saved search's `query` + `filters`. Each row uses the same `Trace` shape as `listTraces` — use the trace point-lookup endpoints (`getTrace`, `listTraceSpans`, `getTraceSpan`, `listTraceAnnotations`) to drill into individual traces.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        search_slug : str
            Saved-search slug (human-readable identifier within the project).

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        sort_by : typing.Optional[ListTracesSavedSearchesRequestSortBy]
            Field to sort by. Defaults to `startTime`. Pass `relevance` to rank by semantic match against the saved search's query (best match first, then most recent).

        sort_direction : typing.Optional[ListTracesSavedSearchesRequestSortDirection]
            Sort direction. Defaults to `desc` (most recent first).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedTraces
            Page of traces

        Examples
        --------
        from latitude_sdk import LatitudeClient

        client = LatitudeClient(
            api_key="YOUR_API_KEY",
        )
        client.saved_searches.list_traces(
            project_slug="projectSlug",
            search_slug="searchSlug",
        )
        """
        _response = self._raw_client.list_traces(
            project_slug,
            search_slug,
            cursor=cursor,
            limit=limit,
            sort_by=sort_by,
            sort_direction=sort_direction,
            request_options=request_options,
        )
        return _response.data


class AsyncSavedSearchesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawSavedSearchesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawSavedSearchesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawSavedSearchesClient
        """
        return self._raw_client

    async def list(
        self, project_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> PaginatedSavedSearches:
        """
        Returns every saved search in the project. The response uses the standard paginated shape; the saved-search list currently fits in a single page (`nextCursor` is always `null`).

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedSavedSearches
            List of saved searches

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.saved_searches.list(
                project_slug="projectSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(project_slug, request_options=request_options)
        return _response.data

    async def create(
        self,
        project_slug: str,
        *,
        name: str,
        query: typing.Optional[str] = OMIT,
        filters: typing.Optional[typing.Dict[str, typing.Sequence[FilterCondition]]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSearch:
        """
        Creates a saved search within the project. At least one of `query` or `filters` must be set. The slug is derived from `name`. OAuth-authenticated only.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        name : str
            Human-readable name. Used to derive the slug.

        query : typing.Optional[str]
            Free-text semantic query. `null` (default) when the search is filter-only. At least one of `query` or `filters` must be set.

        filters : typing.Optional[typing.Dict[str, typing.Sequence[FilterCondition]]]
            Structured filter set. Defaults to `{}` (no filters). At least one of `query` or `filters` must be set.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSearch
            Saved search created

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.saved_searches.create(
                project_slug="projectSlug",
                name="name",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.create(
            project_slug, name=name, query=query, filters=filters, request_options=request_options
        )
        return _response.data

    async def get(
        self, project_slug: str, search_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SavedSearch:
        """
        Returns a single saved search by slug.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        search_slug : str
            Saved-search slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSearch
            Saved search

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.saved_searches.get(
                project_slug="projectSlug",
                search_slug="searchSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(project_slug, search_slug, request_options=request_options)
        return _response.data

    async def delete(
        self, project_slug: str, search_slug: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> None:
        """
        Deletes a saved search by slug.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        search_slug : str
            Saved-search slug (human-readable identifier within the project).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        None

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.saved_searches.delete(
                project_slug="projectSlug",
                search_slug="searchSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.delete(project_slug, search_slug, request_options=request_options)
        return _response.data

    async def update(
        self,
        project_slug: str,
        search_slug: str,
        *,
        name: typing.Optional[str] = OMIT,
        query: typing.Optional[str] = OMIT,
        filters: typing.Optional[typing.Dict[str, typing.Sequence[FilterCondition]]] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> SavedSearch:
        """
        Updates a saved search. Renaming may regenerate the slug — clients should re-read the response or rely on the `id` for stable references.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        search_slug : str
            Saved-search slug (human-readable identifier within the project).

        name : typing.Optional[str]
            New human-readable name. Triggers slug regeneration when the change affects the slug form (cosmetic edits like capitalization keep the URL stable).

        query : typing.Optional[str]
            Replace the free-text query. Pass `null` to clear it.

        filters : typing.Optional[typing.Dict[str, typing.Sequence[FilterCondition]]]
            Replace the structured filter set.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SavedSearch
            Updated saved search

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.saved_searches.update(
                project_slug="projectSlug",
                search_slug="searchSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.update(
            project_slug, search_slug, name=name, query=query, filters=filters, request_options=request_options
        )
        return _response.data

    async def list_traces(
        self,
        project_slug: str,
        search_slug: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        sort_by: typing.Optional[ListTracesSavedSearchesRequestSortBy] = None,
        sort_direction: typing.Optional[ListTracesSavedSearchesRequestSortDirection] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedTraces:
        """
        Returns a cursor-paginated page of traces that match the saved search's `query` + `filters`. Each row uses the same `Trace` shape as `listTraces` — use the trace point-lookup endpoints (`getTrace`, `listTraceSpans`, `getTraceSpan`, `listTraceAnnotations`) to drill into individual traces.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        search_slug : str
            Saved-search slug (human-readable identifier within the project).

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        sort_by : typing.Optional[ListTracesSavedSearchesRequestSortBy]
            Field to sort by. Defaults to `startTime`. Pass `relevance` to rank by semantic match against the saved search's query (best match first, then most recent).

        sort_direction : typing.Optional[ListTracesSavedSearchesRequestSortDirection]
            Sort direction. Defaults to `desc` (most recent first).

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedTraces
            Page of traces

        Examples
        --------
        import asyncio

        from latitude_sdk import AsyncLatitudeClient

        client = AsyncLatitudeClient(
            api_key="YOUR_API_KEY",
        )


        async def main() -> None:
            await client.saved_searches.list_traces(
                project_slug="projectSlug",
                search_slug="searchSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_traces(
            project_slug,
            search_slug,
            cursor=cursor,
            limit=limit,
            sort_by=sort_by,
            sort_direction=sort_direction,
            request_options=request_options,
        )
        return _response.data
