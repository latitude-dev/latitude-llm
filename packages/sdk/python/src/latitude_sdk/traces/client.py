# This file was auto-generated by Fern from our API Definition.

import datetime as dt
import typing

from ..core.client_wrapper import AsyncClientWrapper, SyncClientWrapper
from ..core.request_options import RequestOptions
from ..types.annotation import Annotation
from ..types.export_traces_response import ExportTracesResponse
from ..types.filter_set import FilterSet
from ..types.paginated_trace_annotations import PaginatedTraceAnnotations
from ..types.paginated_traces import PaginatedTraces
from ..types.span_detail import SpanDetail
from ..types.trace_analytics_response import TraceAnalyticsResponse
from ..types.trace_detail import TraceDetail
from ..types.trace_spans import TraceSpans
from ..types.traces_ref import TracesRef
from .raw_client import AsyncRawTracesClient, RawTracesClient
from .types.list_traces_body_sort_by import ListTracesBodySortBy
from .types.list_traces_body_sort_direction import ListTracesBodySortDirection

# this is used as the default value for optional parameters
OMIT = typing.cast(typing.Any, ...)


class TracesClient:
    def __init__(self, *, client_wrapper: SyncClientWrapper):
        self._raw_client = RawTracesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> RawTracesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        RawTracesClient
        """
        return self._raw_client

    def list(
        self,
        project_slug: str,
        *,
        cursor: typing.Optional[str] = OMIT,
        limit: typing.Optional[int] = OMIT,
        sort_by: typing.Optional[ListTracesBodySortBy] = OMIT,
        sort_direction: typing.Optional[ListTracesBodySortDirection] = OMIT,
        query: typing.Optional[str] = OMIT,
        filters: typing.Optional[FilterSet] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedTraces:
        """
        Returns a cursor-paginated page of traces in the project. Combine `filters` with `query` (free-text semantic search) to narrow the result set. Trace list rows exclude per-message LLM content — use `getTrace` for the full conversation view.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        sort_by : typing.Optional[ListTracesBodySortBy]
            Field to sort by. Defaults to `startTime`. Pass `relevance` together with `query` to rank by semantic match (best match first, then most recent).

        sort_direction : typing.Optional[ListTracesBodySortDirection]
            Sort direction. Defaults to `desc` (most recent first).

        query : typing.Optional[str]
            Free-text semantic search across the trace's input and output messages. Combined with `filters` via AND.

        filters : typing.Optional[FilterSet]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedTraces
            Page of traces

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.traces.list(
            project_slug="projectSlug",
        )
        """
        _response = self._raw_client.list(
            project_slug,
            cursor=cursor,
            limit=limit,
            sort_by=sort_by,
            sort_direction=sort_direction,
            query=query,
            filters=filters,
            request_options=request_options,
        )
        return _response.data

    def analytics(
        self,
        project_slug: str,
        *,
        from_iso: typing.Optional[dt.datetime] = None,
        to_iso: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> TraceAnalyticsResponse:
        """
        Returns trace analytics for the project: a total (or median) per metric over the requested range, plus a per-bucket series for each metric. Buckets are 12-hour UTC-aligned. The range defaults to the trailing 7 days.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        from_iso : typing.Optional[dt.datetime]
            Lower bound (inclusive) of the time range. Defaults to 7 days before `toIso`.

        to_iso : typing.Optional[dt.datetime]
            Upper bound (inclusive) of the time range. Defaults to now.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        TraceAnalyticsResponse
            Trace analytics

        Examples
        --------
        import datetime

        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.traces.analytics(
            project_slug="projectSlug",
            from_iso=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
            to_iso=datetime.datetime.fromisoformat(
                "2024-01-15 09:30:00+00:00",
            ),
        )
        """
        _response = self._raw_client.analytics(
            project_slug, from_iso=from_iso, to_iso=to_iso, request_options=request_options
        )
        return _response.data

    def get(
        self, project_slug: str, trace_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> TraceDetail:
        """
        Returns a single trace by id, including the captured system instructions and the conversation messages from the trace's last LLM-completion span.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        trace_id : str
            32-character trace identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        TraceDetail
            Trace detail

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.traces.get(
            project_slug="projectSlug",
            trace_id="traceId",
        )
        """
        _response = self._raw_client.get(project_slug, trace_id, request_options=request_options)
        return _response.data

    def list_spans(
        self, project_slug: str, trace_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> TraceSpans:
        """
        Returns every span belonging to the trace, ordered by `startTime` ascending. Spans carry the OpenTelemetry envelope (kind, status, attributes, resource) plus Latitude's GenAI enrichment (tokens, cost, operation, provider, model). Per-message LLM content is excluded for size; use a span point-lookup for the conversation payload.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        trace_id : str
            32-character trace identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        TraceSpans
            Spans of the trace

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.traces.list_spans(
            project_slug="projectSlug",
            trace_id="traceId",
        )
        """
        _response = self._raw_client.list_spans(project_slug, trace_id, request_options=request_options)
        return _response.data

    def get_span(
        self, project_slug: str, trace_id: str, span_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SpanDetail:
        """
        Returns one span by id, including the LLM conversation (system instructions, input messages, output messages), tool data (definitions, call id, input, output), and the full OpenTelemetry payload (attributes, resource, events, links) that's excluded from the lighter list shape.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        trace_id : str
            32-character trace identifier.

        span_id : str
            16-character span identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SpanDetail
            Span detail

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.traces.get_span(
            project_slug="projectSlug",
            trace_id="traceId",
            span_id="spanId",
        )
        """
        _response = self._raw_client.get_span(project_slug, trace_id, span_id, request_options=request_options)
        return _response.data

    def list_annotations(
        self,
        project_slug: str,
        trace_id: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedTraceAnnotations:
        """
        Returns a cursor-paginated page of annotations pinned to the trace, including both published annotations and drafts.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        trace_id : str
            32-character trace identifier.

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedTraceAnnotations
            Annotations of the trace

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.traces.list_annotations(
            project_slug="projectSlug",
            trace_id="traceId",
            cursor="cursor",
            limit=1,
        )
        """
        _response = self._raw_client.list_annotations(
            project_slug, trace_id, cursor=cursor, limit=limit, request_options=request_options
        )
        return _response.data

    def get_annotation(
        self,
        project_slug: str,
        trace_id: str,
        annotation_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Annotation:
        """
        Returns one annotation by id pinned to the trace.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        trace_id : str
            32-character trace identifier.

        annotation_id : str
            Stable annotation identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Annotation
            Annotation

        Examples
        --------
        from latitude import LatitudeApiClient

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.traces.get_annotation(
            project_slug="projectSlug",
            trace_id="traceId",
            annotation_id="annotationId",
        )
        """
        _response = self._raw_client.get_annotation(
            project_slug, trace_id, annotation_id, request_options=request_options
        )
        return _response.data

    def export(
        self,
        project_slug: str,
        *,
        traces: TracesRef,
        recipient: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ExportTracesResponse:
        """
        Enqueues a CSV export of the traces matched by `traces`. The export runs asynchronously; a download link is emailed to `recipient` when the file is ready. The response returns immediately with `status = "queued"`. The recipient must already be a member of the requesting organization.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        traces : TracesRef

        recipient : str
            Email address the export download link is sent to. Must belong to a member of the requesting organization — otherwise the request is rejected with 400.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ExportTracesResponse
            Export enqueued

        Examples
        --------
        from latitude import LatitudeApiClient, TracesRef_Ids

        client = LatitudeApiClient(
            token="YOUR_TOKEN",
        )
        client.traces.export(
            project_slug="projectSlug",
            traces=TracesRef_Ids(
                ids=["ids"],
            ),
            recipient="recipient",
        )
        """
        _response = self._raw_client.export(
            project_slug, traces=traces, recipient=recipient, request_options=request_options
        )
        return _response.data


class AsyncTracesClient:
    def __init__(self, *, client_wrapper: AsyncClientWrapper):
        self._raw_client = AsyncRawTracesClient(client_wrapper=client_wrapper)

    @property
    def with_raw_response(self) -> AsyncRawTracesClient:
        """
        Retrieves a raw implementation of this client that returns raw responses.

        Returns
        -------
        AsyncRawTracesClient
        """
        return self._raw_client

    async def list(
        self,
        project_slug: str,
        *,
        cursor: typing.Optional[str] = OMIT,
        limit: typing.Optional[int] = OMIT,
        sort_by: typing.Optional[ListTracesBodySortBy] = OMIT,
        sort_direction: typing.Optional[ListTracesBodySortDirection] = OMIT,
        query: typing.Optional[str] = OMIT,
        filters: typing.Optional[FilterSet] = OMIT,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedTraces:
        """
        Returns a cursor-paginated page of traces in the project. Combine `filters` with `query` (free-text semantic search) to narrow the result set. Trace list rows exclude per-message LLM content — use `getTrace` for the full conversation view.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        sort_by : typing.Optional[ListTracesBodySortBy]
            Field to sort by. Defaults to `startTime`. Pass `relevance` together with `query` to rank by semantic match (best match first, then most recent).

        sort_direction : typing.Optional[ListTracesBodySortDirection]
            Sort direction. Defaults to `desc` (most recent first).

        query : typing.Optional[str]
            Free-text semantic search across the trace's input and output messages. Combined with `filters` via AND.

        filters : typing.Optional[FilterSet]

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedTraces
            Page of traces

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.traces.list(
                project_slug="projectSlug",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list(
            project_slug,
            cursor=cursor,
            limit=limit,
            sort_by=sort_by,
            sort_direction=sort_direction,
            query=query,
            filters=filters,
            request_options=request_options,
        )
        return _response.data

    async def analytics(
        self,
        project_slug: str,
        *,
        from_iso: typing.Optional[dt.datetime] = None,
        to_iso: typing.Optional[dt.datetime] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> TraceAnalyticsResponse:
        """
        Returns trace analytics for the project: a total (or median) per metric over the requested range, plus a per-bucket series for each metric. Buckets are 12-hour UTC-aligned. The range defaults to the trailing 7 days.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        from_iso : typing.Optional[dt.datetime]
            Lower bound (inclusive) of the time range. Defaults to 7 days before `toIso`.

        to_iso : typing.Optional[dt.datetime]
            Upper bound (inclusive) of the time range. Defaults to now.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        TraceAnalyticsResponse
            Trace analytics

        Examples
        --------
        import asyncio
        import datetime

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.traces.analytics(
                project_slug="projectSlug",
                from_iso=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
                to_iso=datetime.datetime.fromisoformat(
                    "2024-01-15 09:30:00+00:00",
                ),
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.analytics(
            project_slug, from_iso=from_iso, to_iso=to_iso, request_options=request_options
        )
        return _response.data

    async def get(
        self, project_slug: str, trace_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> TraceDetail:
        """
        Returns a single trace by id, including the captured system instructions and the conversation messages from the trace's last LLM-completion span.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        trace_id : str
            32-character trace identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        TraceDetail
            Trace detail

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.traces.get(
                project_slug="projectSlug",
                trace_id="traceId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get(project_slug, trace_id, request_options=request_options)
        return _response.data

    async def list_spans(
        self, project_slug: str, trace_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> TraceSpans:
        """
        Returns every span belonging to the trace, ordered by `startTime` ascending. Spans carry the OpenTelemetry envelope (kind, status, attributes, resource) plus Latitude's GenAI enrichment (tokens, cost, operation, provider, model). Per-message LLM content is excluded for size; use a span point-lookup for the conversation payload.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        trace_id : str
            32-character trace identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        TraceSpans
            Spans of the trace

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.traces.list_spans(
                project_slug="projectSlug",
                trace_id="traceId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_spans(project_slug, trace_id, request_options=request_options)
        return _response.data

    async def get_span(
        self, project_slug: str, trace_id: str, span_id: str, *, request_options: typing.Optional[RequestOptions] = None
    ) -> SpanDetail:
        """
        Returns one span by id, including the LLM conversation (system instructions, input messages, output messages), tool data (definitions, call id, input, output), and the full OpenTelemetry payload (attributes, resource, events, links) that's excluded from the lighter list shape.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        trace_id : str
            32-character trace identifier.

        span_id : str
            16-character span identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        SpanDetail
            Span detail

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.traces.get_span(
                project_slug="projectSlug",
                trace_id="traceId",
                span_id="spanId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_span(project_slug, trace_id, span_id, request_options=request_options)
        return _response.data

    async def list_annotations(
        self,
        project_slug: str,
        trace_id: str,
        *,
        cursor: typing.Optional[str] = None,
        limit: typing.Optional[int] = None,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> PaginatedTraceAnnotations:
        """
        Returns a cursor-paginated page of annotations pinned to the trace, including both published annotations and drafts.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        trace_id : str
            32-character trace identifier.

        cursor : typing.Optional[str]
            Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page.

        limit : typing.Optional[int]
            Page size. Defaults to 50; max 200.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        PaginatedTraceAnnotations
            Annotations of the trace

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.traces.list_annotations(
                project_slug="projectSlug",
                trace_id="traceId",
                cursor="cursor",
                limit=1,
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.list_annotations(
            project_slug, trace_id, cursor=cursor, limit=limit, request_options=request_options
        )
        return _response.data

    async def get_annotation(
        self,
        project_slug: str,
        trace_id: str,
        annotation_id: str,
        *,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> Annotation:
        """
        Returns one annotation by id pinned to the trace.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        trace_id : str
            32-character trace identifier.

        annotation_id : str
            Stable annotation identifier.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        Annotation
            Annotation

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.traces.get_annotation(
                project_slug="projectSlug",
                trace_id="traceId",
                annotation_id="annotationId",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.get_annotation(
            project_slug, trace_id, annotation_id, request_options=request_options
        )
        return _response.data

    async def export(
        self,
        project_slug: str,
        *,
        traces: TracesRef,
        recipient: str,
        request_options: typing.Optional[RequestOptions] = None,
    ) -> ExportTracesResponse:
        """
        Enqueues a CSV export of the traces matched by `traces`. The export runs asynchronously; a download link is emailed to `recipient` when the file is ready. The response returns immediately with `status = "queued"`. The recipient must already be a member of the requesting organization.

        Parameters
        ----------
        project_slug : str
            Project slug (human-readable identifier)

        traces : TracesRef

        recipient : str
            Email address the export download link is sent to. Must belong to a member of the requesting organization — otherwise the request is rejected with 400.

        request_options : typing.Optional[RequestOptions]
            Request-specific configuration.

        Returns
        -------
        ExportTracesResponse
            Export enqueued

        Examples
        --------
        import asyncio

        from latitude import AsyncLatitudeApiClient, TracesRef_Ids

        client = AsyncLatitudeApiClient(
            token="YOUR_TOKEN",
        )


        async def main() -> None:
            await client.traces.export(
                project_slug="projectSlug",
                traces=TracesRef_Ids(
                    ids=["ids"],
                ),
                recipient="recipient",
            )


        asyncio.run(main())
        """
        _response = await self._raw_client.export(
            project_slug, traces=traces, recipient=recipient, request_options=request_options
        )
        return _response.data
