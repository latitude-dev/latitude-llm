# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import UniversalBaseModel


class AccountUser(UniversalBaseModel):
    """
    The user the request is acting on behalf of. `null` for API-key callers — API keys are org-scoped, not user-scoped.
    """

    id: str = pydantic.Field()
    """
    Stable user identifier across the API.
    """

    email: str = pydantic.Field()
    """
    Verified email address of the authenticated user.
    """

    name: typing.Optional[str] = pydantic.Field(default=None)
    """
    Display name, when set. `null` until the user completes onboarding.
    """

    image: typing.Optional[str] = pydantic.Field(default=None)
    """
    Profile image URL, when set.
    """

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)
