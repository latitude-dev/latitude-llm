# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
from ..core.pydantic_utilities import UniversalBaseModel
from .session_assessment_estimate_range_interpretation import SessionAssessmentEstimateRangeInterpretation


class SessionAssessmentEstimateRange(UniversalBaseModel):
    """
    Impact in the family's own unit. Never a score: sessions do not receive Cost numbers.
    """

    unit: str = pydantic.Field()
    """
    Unit both the point estimate and its bounds are expressed in.
    """

    point: float = pydantic.Field()
    """
    Point estimate in `unit`.
    """

    lower: typing.Optional[float] = pydantic.Field(default=None)
    """
    Lower bound in `unit`, when the estimate is bounded.
    """

    upper: typing.Optional[float] = pydantic.Field(default=None)
    """
    Upper bound in `unit`, when the estimate is bounded.
    """

    interpretation: typing.Optional[SessionAssessmentEstimateRangeInterpretation] = pydantic.Field(default=None)
    """
    What the bounds mean. `identificationBound` is the range the evidence cannot narrow further; `confidenceInterval` is sampling uncertainty. Present whenever a bound is.
    """

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)
