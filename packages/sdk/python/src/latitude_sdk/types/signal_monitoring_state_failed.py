# This file was auto-generated by Fern from our API Definition.

import typing

import pydantic
import typing_extensions
from ..core.pydantic_utilities import UniversalBaseModel
from ..core.serialization import FieldMetadata
from .signal_monitoring_state_failed_phase import SignalMonitoringStateFailedPhase


class SignalMonitoringStateFailed(UniversalBaseModel):
    """
    The most recent generation or realignment workflow for this signal ended in failure.
    """

    phase: SignalMonitoringStateFailedPhase = pydantic.Field()
    """
    Which workflow failed: `generate` (creating the evaluation) or `realign` (updating it).
    """

    evaluation_id: typing_extensions.Annotated[
        typing.Optional[str],
        FieldMetadata(alias="evaluationId"),
        pydantic.Field(
            alias="evaluationId",
            default=None,
            description="Id of the evaluation whose realignment failed. Absent when the failure was during `generate`.",
        ),
    ]
    reason: typing.Optional[str] = pydantic.Field(default=None)
    """
    Resolved failure message, or `null` once Temporal has dropped the failed run.
    """

    model_config: typing.ClassVar[pydantic.ConfigDict] = pydantic.ConfigDict(extra="allow", frozen=True)
