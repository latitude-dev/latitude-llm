// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../../../../index.js";

/**
 * @example
 *     {
 *         name: "name"
 *     }
 */
export interface CreateExperimentBody {
    /** Human-readable name. Used to derive the slug. */
    name: string;
    /** Optional free-form description. */
    description?: string;
    /** Variant definitions. Omit to seed two default variants (`Variant A` baseline + `Variant B`); pass `[]` to create an empty experiment. */
    variants?: CreateExperimentBody.Variants.Item[];
}

export namespace CreateExperimentBody {
    export type Variants = Variants.Item[];

    export namespace Variants {
        export interface Item {
            /** Existing variant id to preserve. Omit to mint a new variant. */
            id?: string | undefined;
            /** Variant name. Unique within the experiment. */
            name: string;
            /** `true` for the baseline variant. Exactly one variant must be the baseline. */
            baseline: boolean;
            /** Session filters selecting this variant's population. */
            filterSet: Record<string, Latitude.FilterCondition[]>;
            /** Free-text / semantic search, or `null`. */
            query: string | null;
            /** Time window, or `null` for the default last-30-days window. */
            timeRange: Item.TimeRange | null;
        }

        export namespace Item {
            /**
             * Time window, or `null` for the default last-30-days window.
             */
            export type TimeRange =
                | {
                      type: "relative";
                      seconds: number;
                  }
                | {
                      type: "absolute";
                      fromIso: string;
                      toIso: string;
                  };
        }
    }
}
