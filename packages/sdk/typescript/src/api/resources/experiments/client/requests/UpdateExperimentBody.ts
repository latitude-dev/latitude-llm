// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../../../../index.js";

/**
 * @example
 *     {}
 */
export interface UpdateExperimentBody {
    /** New name. Renaming may regenerate the slug — re-read the response or rely on `id`. */
    name?: string;
    /** New description. */
    description?: string;
    /** Full replacement of the variants array. Each variant carries its own `baseline` flag. */
    variants?: UpdateExperimentBody.Variants.Item[];
}

export namespace UpdateExperimentBody {
    export type Variants = Variants.Item[];

    export namespace Variants {
        export interface Item {
            /** Existing variant id to preserve. Omit to mint a new variant. */
            id?: string | undefined;
            /** Variant name. Unique within the experiment. */
            name: string;
            /** `true` for the baseline variant. Exactly one variant must be the baseline. */
            baseline: boolean;
            /** Session filters selecting this variant's population. */
            filterSet: Record<string, Latitude.FilterCondition[]>;
            /** Free-text / semantic search, or `null`. */
            query: string | null;
            /** Time window, or `null` for the default last-30-days window. */
            timeRange: Item.TimeRange | null;
        }

        export namespace Item {
            /**
             * Time window, or `null` for the default last-30-days window.
             */
            export type TimeRange =
                | {
                      type: "relative";
                      seconds: number;
                  }
                | {
                      type: "absolute";
                      fromIso: string;
                      toIso: string;
                  };
        }
    }
}
