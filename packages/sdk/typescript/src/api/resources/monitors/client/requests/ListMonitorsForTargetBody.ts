// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../../../../index.js";

/**
 * @example
 *     {
 *         filterSetContains: {
 *             "key": [{
 *                     op: "eq",
 *                     value: "value"
 *                 }]
 *         }
 *     }
 */
export interface ListMonitorsForTargetBody {
    /** Optional target type to match. */
    targetType?: ListMonitorsForTargetBody.TargetType;
    /** Filter subset to match against monitor targets. For one user use `userId`; for one tool use `operation = execute_tool` and `toolName`. */
    filterSetContains: Record<string, Latitude.FilterCondition[]>;
}

export namespace ListMonitorsForTargetBody {
    /** Optional target type to match. */
    export const TargetType = {
        SavedSearch: "savedSearch",
        Tool: "tool",
        User: "user",
        Session: "session",
    } as const;
    export type TargetType = (typeof TargetType)[keyof typeof TargetType];
}
