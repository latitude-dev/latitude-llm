// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../../../../index.js";

/**
 * @example
 *     {}
 */
export interface UpdateMonitorBody {
    /** New name. Renaming may regenerate the slug — re-read the response or rely on `id`. */
    name?: string;
    /** New description. */
    description?: string;
    /** Replacement target watched by the monitor. */
    target?: UpdateMonitorBody.Target;
    /** Replacement incident trigger for the monitor rule. */
    trigger?: UpdateMonitorBody.Trigger;
    /** Replacement metric evaluated by the monitor. */
    metric?: Latitude.UpdateMonitorBodyMetric;
    /** Replacement condition for threshold or escalating monitors. */
    condition?: Latitude.UpdateMonitorBodyCondition;
    /** Replacement incident severity. */
    severity?: UpdateMonitorBody.Severity;
}

export namespace UpdateMonitorBody {
    /**
     * Replacement target watched by the monitor.
     */
    export interface Target {
        /** Product target category: `savedSearch`, `tool`, `user`, or `session`. */
        type: Target.Type;
        /** Target entity id, or `null` for project-wide targets. */
        id: string | null;
        filterSet?: Latitude.MonitorFilterSet | undefined;
        /** Semantic query applied when evaluating inline trace targets. */
        query?: (string | null) | undefined;
        /** Normalized target kind returned on persisted monitors. */
        kind?: Target.Kind | undefined;
        /** Telemetry stream evaluated by the monitor. */
        stream?: Target.Stream | undefined;
        /** Saved-search id for saved-search monitors, or `null` for inline targets. */
        savedSearchId?: (string | null) | undefined;
        /** Default metric evaluated for this target. */
        metric?: Latitude.UpdateMonitorBodyTargetMetric | undefined;
    }

    export namespace Target {
        /** Product target category: `savedSearch`, `tool`, `user`, or `session`. */
        export const Type = {
            SavedSearch: "savedSearch",
            Tool: "tool",
            User: "user",
            Session: "session",
        } as const;
        export type Type = (typeof Type)[keyof typeof Type];
        /** Normalized target kind returned on persisted monitors. */
        export const Kind = {
            SavedSearch: "savedSearch",
            Tool: "tool",
            User: "user",
            Session: "session",
        } as const;
        export type Kind = (typeof Kind)[keyof typeof Kind];
        /** Telemetry stream evaluated by the monitor. */
        export const Stream = {
            Traces: "traces",
            Spans: "spans",
            Sessions: "sessions",
        } as const;
        export type Stream = (typeof Stream)[keyof typeof Stream];
    }

    /** Replacement incident trigger for the monitor rule. */
    export const Trigger = {
        Match: "match",
        Threshold: "threshold",
        Escalating: "escalating",
    } as const;
    export type Trigger = (typeof Trigger)[keyof typeof Trigger];
    /** Replacement incident severity. */
    export const Severity = {
        Low: "low",
        Medium: "medium",
        High: "high",
    } as const;
    export type Severity = (typeof Severity)[keyof typeof Severity];
}
