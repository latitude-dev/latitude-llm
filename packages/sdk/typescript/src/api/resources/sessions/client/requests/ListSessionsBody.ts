// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../../../../index.js";

/**
 * @example
 *     {}
 */
export interface ListSessionsBody {
    /** Opaque cursor returned in a previous response's `nextCursor`. Omit on the first page. */
    cursor?: string;
    /** Page size. Defaults to 50; max 200. */
    limit?: number;
    /** Field to sort by. Defaults to `lastActivity` (most recently active first). */
    sortBy?: ListSessionsBody.SortBy;
    /** Sort direction. Defaults to `desc` (most recent first). */
    sortDirection?: ListSessionsBody.SortDirection;
    /** Free-text semantic search across the sessions' traces (input and output messages). Combined with `filters` via AND. */
    query?: string;
    filters?: Latitude.SessionFilterSet;
}

export namespace ListSessionsBody {
    /** Field to sort by. Defaults to `lastActivity` (most recently active first). */
    export const SortBy = {
        LastActivity: "lastActivity",
        StartTime: "startTime",
        Duration: "duration",
        Ttft: "ttft",
        Cost: "cost",
        Spans: "spans",
        TraceCount: "traceCount",
    } as const;
    export type SortBy = (typeof SortBy)[keyof typeof SortBy];
    /** Sort direction. Defaults to `desc` (most recent first). */
    export const SortDirection = {
        Asc: "asc",
        Desc: "desc",
    } as const;
    export type SortDirection = (typeof SortDirection)[keyof typeof SortDirection];
}
