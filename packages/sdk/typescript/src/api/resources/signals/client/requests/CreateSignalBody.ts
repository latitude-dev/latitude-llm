// This file was auto-generated by Fern from our API Definition.

import type * as LatitudeApi from "../../../../index.js";

/**
 * @example
 *     {
 *         name: "name",
 *         description: "description",
 *         evaluation: {
 *             settings: {
 *                 kind: "judge",
 *                 criteria: "criteria"
 *             }
 *         }
 *     }
 */
export interface CreateSignalBody {
    /** Human-readable name. Used to derive the slug. */
    name: string;
    /** What this signal captures. */
    description: string;
    /** Manual triage priority. Null/omitted leaves it unset. */
    priority?: CreateSignalBody.Priority;
    /** Row-local pre-gate restricting which traces the evaluation runs against. Omitted = all traces. */
    filters?: Record<string, LatitudeApi.FilterCondition[]>;
    /** The signal's membership detector. Provide exactly one of `settings` or `script`. */
    evaluation: CreateSignalBody.Evaluation;
}

export namespace CreateSignalBody {
    /** Manual triage priority. Null/omitted leaves it unset. */
    export const Priority = {
        Low: "low",
        Medium: "medium",
        High: "high",
        Urgent: "urgent",
    } as const;
    export type Priority = (typeof Priority)[keyof typeof Priority];
    /**
     * The signal's membership detector. Provide exactly one of `settings` or `script`.
     */
    export type Evaluation =
        | {
              settings: LatitudeApi.CreateSignalBodyEvaluationSettingsSettings;
          }
        | {
              script: string;
          };
}
