// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export interface AgentScoreDimensionAttribution {
    /** Dimension explained. */
    scoreDimension: AgentScoreDimensionAttribution.ScoreDimension;
    /** Causes, most responsible first. */
    rows: Latitude.AgentScoreCauseRow[];
    /** Points of the deficit no named cause accounts for, including estimation error. */
    residual: number;
    /** The dimension's whole distance from healthy, in points. */
    totalDeficit: number;
    /** `sampled` when there were too many causes to attribute exactly. */
    method: AgentScoreDimensionAttribution.Method;
    /** Standard error of the sampled shares in points, or `null` when attribution was exact. */
    approximationError: number | null;
}

export namespace AgentScoreDimensionAttribution {
    /** Dimension explained. */
    export const ScoreDimension = {
        Outcome: "outcome",
        Reliability: "reliability",
        Cost: "cost",
        Speed: "speed",
        Safety: "safety",
    } as const;
    export type ScoreDimension = (typeof ScoreDimension)[keyof typeof ScoreDimension];
    /** `sampled` when there were too many causes to attribute exactly. */
    export const Method = {
        Exact: "exact",
        Sampled: "sampled",
    } as const;
    export type Method = (typeof Method)[keyof typeof Method];
}
