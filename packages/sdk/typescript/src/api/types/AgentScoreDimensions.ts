// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export interface AgentScoreDimensions {
    outcome: Latitude.AgentScoreDimension;
    /** Chance the agent completes a reference run of 20 sessions without a terminal operational failure. */
    reliability: AgentScoreDimensions.Reliability;
    /** Health of the agent's use of paid and token-bearing resources. */
    cost: AgentScoreDimensions.Cost;
    /** Share of user-visible critical-path time that was necessary. */
    speed: AgentScoreDimensions.Speed;
    /** Chance a reference run of 100 sessions contains no confirmed agent-caused harm. */
    safety: AgentScoreDimensions.Safety;
}

export namespace AgentScoreDimensions {
    /**
     * Chance the agent completes a reference run of 20 sessions without a terminal operational failure.
     */
    export interface Reliability {
        /** Dimension score from 0 to 100. Read it with the dimension's own meaning. */
        score: number;
        interval: Latitude.AgentScoreInterval;
    }

    /**
     * Health of the agent's use of paid and token-bearing resources.
     */
    export interface Cost {
        /** Dimension score from 0 to 100. Read it with the dimension's own meaning. */
        score: number;
        interval: Latitude.AgentScoreInterval;
    }

    /**
     * Share of user-visible critical-path time that was necessary.
     */
    export interface Speed {
        /** Dimension score from 0 to 100. Read it with the dimension's own meaning. */
        score: number;
        interval: Latitude.AgentScoreInterval;
    }

    /**
     * Chance a reference run of 100 sessions contains no confirmed agent-caused harm.
     */
    export interface Safety {
        /** Dimension score from 0 to 100. Read it with the dimension's own meaning. */
        score: number;
        interval: Latitude.AgentScoreInterval;
    }
}
