// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

/**
 * The evidence, or `null` when it is not ready.
 */
export interface AgentScoreExplanation {
    /** When this evidence was read, as an ISO-8601 timestamp. It explains present behaviour, not the stored score. */
    computedAt: string;
    /** Scoring version the evidence was read under. */
    scoringVersion: string;
    /** Length in days of the window the evidence covers. */
    windowDays: number;
    /** Production sessions in that window. */
    eligibleSessionCount: number;
    /** Per-dimension cause rows for the dimensions that support a counterfactual. */
    attribution: Latitude.AgentScoreDimensionAttribution[];
    /** Where task failures concentrate. Reach only; these rows carry no share of the score. */
    outcomeIssues: Latitude.AgentScoreIssueRow[];
    /** Issues where the agent caused confirmed harm. */
    safetyConfirmedHarm: Latitude.AgentScoreIssueRow[];
    /** Hostile or sensitive content the agent received. Exposure is context and never lowers Safety. */
    safetyExposure: Latitude.AgentScoreIssueRow[];
}
