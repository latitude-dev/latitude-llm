// This file was auto-generated by Fern from our API Definition.

import type * as LatitudeApi from "../index.js";

/**
 * How the match count/rate is compared.
 */
export type AlertCountThreshold =
    | LatitudeApi.AlertCountThreshold.Absolute
    | LatitudeApi.AlertCountThreshold.Multiplier
    | LatitudeApi.AlertCountThreshold.Expected;

export namespace AlertCountThreshold {
    export interface Absolute {
        mode: "absolute";
        /** Number of matching traces that opens the incident. */
        count: number;
    }

    export interface Multiplier {
        mode: "multiplier";
        /** Multiple of the baseline rate that opens the incident (e.g. `3` = 3×). */
        factor: number;
        baseline: LatitudeApi.AlertBaseline;
    }

    export interface Expected {
        mode: "expected";
        /** Detector sensitivity from 1 (noisiest) to 6 (strictest). Defaults to 3 when omitted. */
        sensitivity?: number | undefined;
    }
}
