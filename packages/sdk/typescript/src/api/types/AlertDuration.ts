// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

/**
 * Length of the window used to compute the baseline rate.
 */
export type AlertDuration = Latitude.AlertDuration.Minutes | Latitude.AlertDuration.Hours | Latitude.AlertDuration.Days;

export namespace AlertDuration {
    export interface Minutes {
        unit: "minutes";
        /** Number of minutes. */
        minutes: number;
    }

    export interface Hours {
        unit: "hours";
        /** Number of hours. */
        hours: number;
    }

    export interface Days {
        unit: "days";
        /** Number of days. */
        days: number;
    }
}
