// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

/**
 * How the metric is compared.
 */
export type AlertMetricThreshold =
    | Latitude.AlertMetricThreshold.Absolute
    | Latitude.AlertMetricThreshold.Multiplier
    | Latitude.AlertMetricThreshold.Expected;

export namespace AlertMetricThreshold {
    export interface Absolute {
        mode: "absolute";
        /** Metric value that opens the incident. */
        value: number;
    }

    export interface Multiplier {
        mode: "multiplier";
        /** Multiple of the baseline rate that opens the incident (e.g. `3` = 3×). */
        factor: number;
        baseline: Latitude.AlertBaseline;
    }

    export interface Expected {
        mode: "expected";
        /** Detector sensitivity from 1 (noisiest) to 6 (strictest). Defaults to 3 when omitted. */
        sensitivity?: number | undefined;
    }
}
