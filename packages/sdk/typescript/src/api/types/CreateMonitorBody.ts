// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export type CreateMonitorBody =
    | Latitude.CreateMonitorBody.Match
    | Latitude.CreateMonitorBody.Threshold
    | Latitude.CreateMonitorBody.Escalating;

export namespace CreateMonitorBody {
    export interface Match {
        trigger: "match";
        /** Human-readable name. Used to derive the slug. */
        name: string;
        /** Optional free-form description. */
        description?: string | undefined;
        /** Entity or filter set watched by the monitor. */
        target: CreateMonitorBodyMatch.Target;
        /** Severity assigned to incidents opened by this monitor. */
        severity: CreateMonitorBodyMatch.Severity;
        /** Metric stored for later rule edits. Match monitors evaluate counts. */
        metric?: Latitude.CreateMonitorBodyMatchMetric | undefined;
    }

    export namespace CreateMonitorBodyMatch {
        /**
         * Entity or filter set watched by the monitor.
         */
        export interface Target {
            /** Product target category: `savedSearch`, `tool`, `user`, or `session`. */
            type: Target.Type;
            /** Target entity id, or `null` for project-wide targets. */
            id: string | null;
            filterSet?: Latitude.MonitorFilterSet | undefined;
            /** Semantic query applied when evaluating inline trace targets. */
            query?: (string | null) | undefined;
            /** Normalized target kind returned on persisted monitors. */
            kind?: Target.Kind | undefined;
            /** Telemetry stream evaluated by the monitor. */
            stream?: Target.Stream | undefined;
            /** Saved-search id for saved-search monitors, or `null` for inline targets. */
            savedSearchId?: (string | null) | undefined;
            /** Default metric evaluated for this target. */
            metric?: Latitude.CreateMonitorBodyMatchTargetMetric | undefined;
        }

        export namespace Target {
            /** Product target category: `savedSearch`, `tool`, `user`, or `session`. */
            export const Type = {
                SavedSearch: "savedSearch",
                Tool: "tool",
                User: "user",
                Session: "session",
            } as const;
            export type Type = (typeof Type)[keyof typeof Type];
            /** Normalized target kind returned on persisted monitors. */
            export const Kind = {
                SavedSearch: "savedSearch",
                Tool: "tool",
                User: "user",
                Session: "session",
            } as const;
            export type Kind = (typeof Kind)[keyof typeof Kind];
            /** Telemetry stream evaluated by the monitor. */
            export const Stream = {
                Traces: "traces",
                Spans: "spans",
                Sessions: "sessions",
            } as const;
            export type Stream = (typeof Stream)[keyof typeof Stream];
        }

        /** Severity assigned to incidents opened by this monitor. */
        export const Severity = {
            Low: "low",
            Medium: "medium",
            High: "high",
        } as const;
        export type Severity = (typeof Severity)[keyof typeof Severity];
    }

    export interface Threshold {
        trigger: "threshold";
        /** Human-readable name. Used to derive the slug. */
        name: string;
        /** Optional free-form description. */
        description?: string | undefined;
        /** Entity or filter set watched by the monitor. */
        target: CreateMonitorBodyThreshold.Target;
        /** Severity assigned to incidents opened by this monitor. */
        severity: CreateMonitorBodyThreshold.Severity;
        /** Metric evaluated by the monitor rule. */
        metric?: Latitude.CreateMonitorBodyThresholdMetric | undefined;
        /** Threshold condition that opens point incidents. */
        condition: CreateMonitorBodyThreshold.Condition;
    }

    export namespace CreateMonitorBodyThreshold {
        /**
         * Entity or filter set watched by the monitor.
         */
        export interface Target {
            /** Product target category: `savedSearch`, `tool`, `user`, or `session`. */
            type: Target.Type;
            /** Target entity id, or `null` for project-wide targets. */
            id: string | null;
            filterSet?: Latitude.MonitorFilterSet | undefined;
            /** Semantic query applied when evaluating inline trace targets. */
            query?: (string | null) | undefined;
            /** Normalized target kind returned on persisted monitors. */
            kind?: Target.Kind | undefined;
            /** Telemetry stream evaluated by the monitor. */
            stream?: Target.Stream | undefined;
            /** Saved-search id for saved-search monitors, or `null` for inline targets. */
            savedSearchId?: (string | null) | undefined;
            /** Default metric evaluated for this target. */
            metric?: Latitude.CreateMonitorBodyThresholdTargetMetric | undefined;
        }

        export namespace Target {
            /** Product target category: `savedSearch`, `tool`, `user`, or `session`. */
            export const Type = {
                SavedSearch: "savedSearch",
                Tool: "tool",
                User: "user",
                Session: "session",
            } as const;
            export type Type = (typeof Type)[keyof typeof Type];
            /** Normalized target kind returned on persisted monitors. */
            export const Kind = {
                SavedSearch: "savedSearch",
                Tool: "tool",
                User: "user",
                Session: "session",
            } as const;
            export type Kind = (typeof Kind)[keyof typeof Kind];
            /** Telemetry stream evaluated by the monitor. */
            export const Stream = {
                Traces: "traces",
                Spans: "spans",
                Sessions: "sessions",
            } as const;
            export type Stream = (typeof Stream)[keyof typeof Stream];
        }

        /** Severity assigned to incidents opened by this monitor. */
        export const Severity = {
            Low: "low",
            Medium: "medium",
            High: "high",
        } as const;
        export type Severity = (typeof Severity)[keyof typeof Severity];

        /**
         * Threshold condition that opens point incidents.
         */
        export interface Condition {
            /** Opens once the measured value crosses the threshold. */
            trigger: Condition.Trigger;
            metric: Latitude.MonitorMetric;
            threshold: Latitude.AlertMetricThreshold;
            /** Direction that opens the incident. Defaults to `above` when omitted. */
            direction?: Condition.Direction | undefined;
        }

        export namespace Condition {
            /** Opens once the measured value crosses the threshold. */
            export const Trigger = {
                Threshold: "threshold",
            } as const;
            export type Trigger = (typeof Trigger)[keyof typeof Trigger];
            /** Direction that opens the incident. Defaults to `above` when omitted. */
            export const Direction = {
                Above: "above",
                Below: "below",
            } as const;
            export type Direction = (typeof Direction)[keyof typeof Direction];
        }
    }

    export interface Escalating {
        trigger: "escalating";
        /** Human-readable name. Used to derive the slug. */
        name: string;
        /** Optional free-form description. */
        description?: string | undefined;
        /** Entity or filter set watched by the monitor. */
        target: CreateMonitorBodyEscalating.Target;
        /** Severity assigned to incidents opened by this monitor. */
        severity: CreateMonitorBodyEscalating.Severity;
        /** Metric evaluated by the monitor rule. */
        metric?: Latitude.CreateMonitorBodyEscalatingMetric | undefined;
        /** Escalating condition that opens sustained incidents. */
        condition: CreateMonitorBodyEscalating.Condition;
    }

    export namespace CreateMonitorBodyEscalating {
        /**
         * Entity or filter set watched by the monitor.
         */
        export interface Target {
            /** Product target category: `savedSearch`, `tool`, `user`, or `session`. */
            type: Target.Type;
            /** Target entity id, or `null` for project-wide targets. */
            id: string | null;
            filterSet?: Latitude.MonitorFilterSet | undefined;
            /** Semantic query applied when evaluating inline trace targets. */
            query?: (string | null) | undefined;
            /** Normalized target kind returned on persisted monitors. */
            kind?: Target.Kind | undefined;
            /** Telemetry stream evaluated by the monitor. */
            stream?: Target.Stream | undefined;
            /** Saved-search id for saved-search monitors, or `null` for inline targets. */
            savedSearchId?: (string | null) | undefined;
            /** Default metric evaluated for this target. */
            metric?: Latitude.CreateMonitorBodyEscalatingTargetMetric | undefined;
        }

        export namespace Target {
            /** Product target category: `savedSearch`, `tool`, `user`, or `session`. */
            export const Type = {
                SavedSearch: "savedSearch",
                Tool: "tool",
                User: "user",
                Session: "session",
            } as const;
            export type Type = (typeof Type)[keyof typeof Type];
            /** Normalized target kind returned on persisted monitors. */
            export const Kind = {
                SavedSearch: "savedSearch",
                Tool: "tool",
                User: "user",
                Session: "session",
            } as const;
            export type Kind = (typeof Kind)[keyof typeof Kind];
            /** Telemetry stream evaluated by the monitor. */
            export const Stream = {
                Traces: "traces",
                Spans: "spans",
                Sessions: "sessions",
            } as const;
            export type Stream = (typeof Stream)[keyof typeof Stream];
        }

        /** Severity assigned to incidents opened by this monitor. */
        export const Severity = {
            Low: "low",
            Medium: "medium",
            High: "high",
        } as const;
        export type Severity = (typeof Severity)[keyof typeof Severity];

        /**
         * Escalating condition that opens sustained incidents.
         */
        export interface Condition {
            /** Opens when the monitor target is escalating or sustained. */
            trigger: Condition.Trigger;
            metric: Latitude.MonitorMetric;
            /** How the metric is compared when threshold-based. */
            threshold?: Latitude.CreateMonitorBodyEscalatingConditionThreshold | undefined;
            /** Direction that opens the incident. Defaults to `above` when omitted. */
            direction?: Condition.Direction | undefined;
            /** Detector sensitivity from 1 (noisiest) to 6 (strictest). Defaults to 3 when omitted. */
            sensitivity?: number | undefined;
            /** Sustained-condition window. */
            window: Condition.Window;
        }

        export namespace Condition {
            /** Opens when the monitor target is escalating or sustained. */
            export const Trigger = {
                Escalating: "escalating",
            } as const;
            export type Trigger = (typeof Trigger)[keyof typeof Trigger];
            /** Direction that opens the incident. Defaults to `above` when omitted. */
            export const Direction = {
                Above: "above",
                Below: "below",
            } as const;
            export type Direction = (typeof Direction)[keyof typeof Direction];

            /**
             * Sustained-condition window.
             */
            export interface Window {
                /** How long the threshold must stay crossed before the incident opens. Minimum 5. */
                minutes: number;
            }
        }
    }
}
