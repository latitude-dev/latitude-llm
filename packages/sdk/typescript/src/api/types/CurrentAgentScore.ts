// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export interface CurrentAgentScore {
    /** Whether a score was published for today. `false` when a dimension did not meet its coverage or confidence floor; no earlier score is substituted. */
    available: boolean;
    /** UTC date the answer refers to, as `YYYY-MM-DD`. */
    date: string;
    /** Today's score, or `null` when none was published. */
    snapshot: CurrentAgentScore.Snapshot | null;
}

export namespace CurrentAgentScore {
    /**
     * Today's score, or `null` when none was published.
     */
    export interface Snapshot {
        /** UTC date the score was published for, as `YYYY-MM-DD`. */
        date: string;
        /** Agent Score from 0 to 100: the weighted mean of the five dimensions for this window. */
        score: number;
        interval: Latitude.AgentScoreInterval;
        dimensions: Latitude.AgentScoreDimensions;
        /** Version of the formulas, prompts and frozen references that produced this score. Scores from different versions are not directly comparable. */
        scoringVersion: string;
        /** Length in days of the rolling window this score covers. */
        windowDays: number;
        /** Production sessions in the window the score was computed over. */
        eligibleSessionCount: number;
        /** Ceiling a safety policy rule applied to the score, or `null` when no rule applied. */
        policyCap: number | null;
    }
}
