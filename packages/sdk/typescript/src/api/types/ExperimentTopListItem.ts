// This file was auto-generated by Fern from our API Definition.

export interface ExperimentTopListItem {
    /** Stable identity: tool name, signal id, or cluster id. */
    key: string;
    /** Human-readable label (resolved name; falls back to `key` when unresolved). */
    label: string;
    /** Ranking value: tool calls / signal occurrences / behaviour observations. */
    value: number;
}
