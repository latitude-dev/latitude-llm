// This file was auto-generated by Fern from our API Definition.

export interface ImportRun {
    /** Whether this page of the import was written. */
    status: ImportRun.Status;
    /** What this page alone brought in. */
    stats: ImportRun.Stats;
    /** Why this page failed, or `null`. */
    error: string | null;
    /** ISO-8601 timestamp at which the page started. */
    startedAt: string;
    /** ISO-8601 timestamp at which the page ended. */
    finishedAt: string;
}

export namespace ImportRun {
    /** Whether this page of the import was written. */
    export const Status = {
        Succeeded: "succeeded",
        Failed: "failed",
    } as const;
    export type Status = (typeof Status)[keyof typeof Status];

    /**
     * What this page alone brought in.
     */
    export interface Stats {
        /** Rows read from the platform, including spans later skipped. */
        recordsFetched: number;
        /** Distinct sessions among the imported traces. */
        sessionsImported: number;
        /** Traces imported. One imported trace bills one credit. */
        tracesImported: number;
        /** Spans written across all imported traces. */
        spansImported: number;
        /** Spans skipped because they carried no usable trace or span id. */
        spansSkipped: number;
    }
}
