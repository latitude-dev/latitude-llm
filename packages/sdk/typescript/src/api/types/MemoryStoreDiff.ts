// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export interface MemoryStoreDiff {
    /** Store the diff was computed for. */
    storeId: string;
    /** Per-record changes between the two points; unchanged records are pruned. */
    changes: Latitude.MemoryStoreChange[];
    /** Total tokens added across all changed records. */
    tokensAdded: number;
    /** Total tokens removed across all changed records. */
    tokensRemoved: number;
    /** Count of changed records per bucket. */
    recordsChanged: MemoryStoreDiff.RecordsChanged;
}

export namespace MemoryStoreDiff {
    /**
     * Count of changed records per bucket.
     */
    export interface RecordsChanged {
        /** Number of records added. */
        added: number;
        /** Number of records updated. */
        updated: number;
        /** Number of records removed. */
        removed: number;
    }
}
