// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

/**
 * Default metric evaluated for this target.
 */
export type MonitorTargetMetric =
    | Latitude.MonitorTargetMetric.Count
    | Latitude.MonitorTargetMetric.ErrorRate
    | Latitude.MonitorTargetMetric.CacheHitRate
    | Latitude.MonitorTargetMetric.Avg
    | Latitude.MonitorTargetMetric.Sum
    | Latitude.MonitorTargetMetric.Min
    | Latitude.MonitorTargetMetric.Max
    | Latitude.MonitorTargetMetric.Median;

export namespace MonitorTargetMetric {
    export interface Count {
        kind: "count";
    }

    export interface ErrorRate {
        kind: "errorRate";
    }

    export interface CacheHitRate {
        kind: "cacheHitRate";
    }

    export interface Avg {
        kind: "avg";
        /** Numeric field to aggregate. */
        field: MonitorTargetMetricAvg.Field;
    }

    export namespace MonitorTargetMetricAvg {
        /** Numeric field to aggregate. */
        export const Field = {
            Duration: "duration",
            Cost: "cost",
            Tokens: "tokens",
        } as const;
        export type Field = (typeof Field)[keyof typeof Field];
    }

    export interface Sum {
        kind: "sum";
        /** Numeric field to aggregate. */
        field: MonitorTargetMetricSum.Field;
    }

    export namespace MonitorTargetMetricSum {
        /** Numeric field to aggregate. */
        export const Field = {
            Duration: "duration",
            Cost: "cost",
            Tokens: "tokens",
        } as const;
        export type Field = (typeof Field)[keyof typeof Field];
    }

    export interface Min {
        kind: "min";
        /** Numeric field to aggregate. */
        field: MonitorTargetMetricMin.Field;
    }

    export namespace MonitorTargetMetricMin {
        /** Numeric field to aggregate. */
        export const Field = {
            Duration: "duration",
            Cost: "cost",
            Tokens: "tokens",
        } as const;
        export type Field = (typeof Field)[keyof typeof Field];
    }

    export interface Max {
        kind: "max";
        /** Numeric field to aggregate. */
        field: MonitorTargetMetricMax.Field;
    }

    export namespace MonitorTargetMetricMax {
        /** Numeric field to aggregate. */
        export const Field = {
            Duration: "duration",
            Cost: "cost",
            Tokens: "tokens",
        } as const;
        export type Field = (typeof Field)[keyof typeof Field];
    }

    export interface Median {
        kind: "median";
        /** Numeric field to aggregate. */
        field: MonitorTargetMetricMedian.Field;
    }

    export namespace MonitorTargetMetricMedian {
        /** Numeric field to aggregate. */
        export const Field = {
            Duration: "duration",
            Cost: "cost",
            Tokens: "tokens",
        } as const;
        export type Field = (typeof Field)[keyof typeof Field];
    }
}
