// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export interface SessionAssessment {
    /** Session represented by this assessment. */
    sessionId: string;
    /** Chronological page of deduplicated evidence items. */
    items: Latitude.SessionAssessmentItem[];
    /** Opaque cursor for the next evidence page. Absent when there are no more items. */
    nextCursor?: string | undefined;
    /** Complete summaries for all five Agent Score dimensions, repeated on every page. */
    dimensions: Latitude.SessionAssessmentDimensionSummary[];
    /** Complete reader coverage for the session, repeated on every page. */
    coverage: SessionAssessment.Coverage;
}

export namespace SessionAssessment {
    /**
     * Complete reader coverage for the session, repeated on every page.
     */
    export interface Coverage {
        /** Coverage status for each evidence reader. */
        readers: Latitude.SessionAssessmentReaderCoverage[];
    }
}
