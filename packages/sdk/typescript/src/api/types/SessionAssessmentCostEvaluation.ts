// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

/**
 * Cost family evaluation for this evidence. Only Cost evidence carries one.
 */
export interface SessionAssessmentCostEvaluation {
    /** Cost family this evidence belongs to: spend, context, tools, memory, or recovery. */
    family: SessionAssessmentCostEvaluation.Family;
    /** The metric's raw reading in `rawUnit`, when measured. */
    rawValue?: number | undefined;
    /** Native unit of `rawValue`. */
    rawUnit?: SessionAssessmentCostEvaluation.RawUnit | undefined;
    /** Whether this evidence was measured, could not be measured, or did not apply. This is not a health label. */
    measurementState: SessionAssessmentCostEvaluation.MeasurementState;
    nativeImpact?: Latitude.SessionAssessmentEstimateRange | undefined;
}

export namespace SessionAssessmentCostEvaluation {
    /** Cost family this evidence belongs to: spend, context, tools, memory, or recovery. */
    export const Family = {
        Spend: "spend",
        Context: "context",
        Tools: "tools",
        Memory: "memory",
        Recovery: "recovery",
    } as const;
    export type Family = (typeof Family)[keyof typeof Family];
    /** Native unit of `rawValue`. */
    export const RawUnit = {
        Microcents: "microcents",
        InputTokens: "inputTokens",
        CacheTokens: "cacheTokens",
        ContextLimitTokens: "contextLimitTokens",
        ToolCalls: "toolCalls",
        MemoryOperations: "memoryOperations",
        MemoryReads: "memoryReads",
        MemoryWrites: "memoryWrites",
        CompletedSessions: "completedSessions",
    } as const;
    export type RawUnit = (typeof RawUnit)[keyof typeof RawUnit];
    /** Whether this evidence was measured, could not be measured, or did not apply. This is not a health label. */
    export const MeasurementState = {
        Measured: "measured",
        Unmeasured: "unmeasured",
        NotApplicable: "notApplicable",
    } as const;
    export type MeasurementState = (typeof MeasurementState)[keyof typeof MeasurementState];
}
