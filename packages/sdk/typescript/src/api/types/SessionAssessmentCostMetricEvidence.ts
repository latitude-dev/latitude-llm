// This file was auto-generated by Fern from our API Definition.

export interface SessionAssessmentCostMetricEvidence {
    /** Stable identifier of the Cost metric. */
    metricId: string;
    /** Cost family this evidence belongs to: spend, context, tools, memory, or recovery. */
    family: SessionAssessmentCostMetricEvidence.Family;
    /** How the aggregate reading was calculated for this session. */
    aggregation: SessionAssessmentCostMetricEvidence.Aggregation;
    /** Whether the metric produced a raw session reading. This is not a health label. */
    measurementState: SessionAssessmentCostMetricEvidence.MeasurementState;
    /** Native unit used by the Cost metric. */
    rawUnit: SessionAssessmentCostMetricEvidence.RawUnit;
    /** Aggregate raw metric value when measured. */
    rawValue?: number | undefined;
    /** Total session units eligible for this metric. */
    eligibleUnits?: number | undefined;
    /** Observed adverse units within `eligibleUnits`; never a calibrated penalty. */
    adverseUnits?: number | undefined;
    /** Whether the reported native impact is directly confirmed or modeled. */
    evidence?: SessionAssessmentCostMetricEvidence.Evidence | undefined;
    /** Impact in the metric's native unit, when available. */
    nativeImpact?: SessionAssessmentCostMetricEvidence.NativeImpact | undefined;
    /** Known limits on how completely the metric could examine the session. */
    limitations: SessionAssessmentCostMetricEvidence.Limitations.Item[];
}

export namespace SessionAssessmentCostMetricEvidence {
    /** Cost family this evidence belongs to: spend, context, tools, memory, or recovery. */
    export const Family = {
        Spend: "spend",
        Context: "context",
        Tools: "tools",
        Memory: "memory",
        Recovery: "recovery",
    } as const;
    export type Family = (typeof Family)[keyof typeof Family];
    /** How the aggregate reading was calculated for this session. */
    export const Aggregation = {
        ResourceRatio: "resourceRatio",
        EventRate: "eventRate",
        SessionMean: "sessionMean",
    } as const;
    export type Aggregation = (typeof Aggregation)[keyof typeof Aggregation];
    /** Whether the metric produced a raw session reading. This is not a health label. */
    export const MeasurementState = {
        Measured: "measured",
        Unmeasured: "unmeasured",
        NotApplicable: "notApplicable",
    } as const;
    export type MeasurementState = (typeof MeasurementState)[keyof typeof MeasurementState];
    /** Native unit used by the Cost metric. */
    export const RawUnit = {
        Microcents: "microcents",
        InputTokens: "inputTokens",
        CacheTokens: "cacheTokens",
        ContextLimitTokens: "contextLimitTokens",
        ToolCalls: "toolCalls",
        MemoryOperations: "memoryOperations",
        MemoryReads: "memoryReads",
        MemoryWrites: "memoryWrites",
        CompletedSessions: "completedSessions",
    } as const;
    export type RawUnit = (typeof RawUnit)[keyof typeof RawUnit];
    /** Whether the reported native impact is directly confirmed or modeled. */
    export const Evidence = {
        Confirmed: "confirmed",
        Modeled: "modeled",
    } as const;
    export type Evidence = (typeof Evidence)[keyof typeof Evidence];

    /**
     * Impact in the metric's native unit, when available.
     */
    export interface NativeImpact {
        /** Unit both the point estimate and its bounds are expressed in. */
        unit: string;
        /** Point estimate in `unit`. */
        point: number;
        /** Lower bound in `unit`, when the estimate is bounded. */
        lower?: number | undefined;
        /** Upper bound in `unit`, when the estimate is bounded. */
        upper?: number | undefined;
        /** What the bounds mean. `identificationBound` is the range the evidence cannot narrow further; `confidenceInterval` is sampling uncertainty. Present whenever a bound is. */
        interpretation?: NativeImpact.Interpretation | undefined;
    }

    export namespace NativeImpact {
        /** What the bounds mean. `identificationBound` is the range the evidence cannot narrow further; `confidenceInterval` is sampling uncertainty. Present whenever a bound is. */
        export const Interpretation = {
            IdentificationBound: "identificationBound",
            ConfidenceInterval: "confidenceInterval",
        } as const;
        export type Interpretation = (typeof Interpretation)[keyof typeof Interpretation];
    }

    export type Limitations = Limitations.Item[];

    export namespace Limitations {
        export const Item = {
            MissingContent: "missingContent",
            TruncatedContent: "truncatedContent",
            MissingPricing: "missingPricing",
            UnknownModelContext: "unknownModelContext",
            UnknownToolContract: "unknownToolContract",
            CriticalPathUnavailable: "criticalPathUnavailable",
            InsufficientComparableCalls: "insufficientComparableCalls",
            DefinitionPeriodIncomplete: "definitionPeriodIncomplete",
        } as const;
        export type Item = (typeof Item)[keyof typeof Item];
    }
}
