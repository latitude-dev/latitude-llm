// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export type SessionAssessmentDimensionEffect =
    | Latitude.SessionAssessmentDimensionEffect.Outcome
    | Latitude.SessionAssessmentDimensionEffect.Reliability
    | Latitude.SessionAssessmentDimensionEffect.Cost
    | Latitude.SessionAssessmentDimensionEffect.Speed
    | Latitude.SessionAssessmentDimensionEffect.Safety;

export namespace SessionAssessmentDimensionEffect {
    export interface Outcome {
        scoreDimension: "outcome";
        /** Evidence about whether the task succeeded. */
        role: SessionAssessmentDimensionEffectOutcome.Role;
        /** How the evidence affects this dimension. */
        direction: SessionAssessmentDimensionEffectOutcome.Direction;
        /** Whether the impact was measured, estimated, or left unquantified. */
        measurement: SessionAssessmentDimensionEffectOutcome.Measurement;
        /** How this evidence may be used in benchmark calculations. */
        benchmarkUse: SessionAssessmentDimensionEffectOutcome.BenchmarkUse;
        impact?: Latitude.SessionAssessmentEvidenceImpact | undefined;
        costEvaluation?: Latitude.SessionAssessmentCostEvaluation | undefined;
    }

    export namespace SessionAssessmentDimensionEffectOutcome {
        /** Evidence about whether the task succeeded. */
        export const Role = {
            TaskOutcome: "taskOutcome",
        } as const;
        export type Role = (typeof Role)[keyof typeof Role];
        /** How the evidence affects this dimension. */
        export const Direction = {
            Positive: "positive",
            Negative: "negative",
            Context: "context",
        } as const;
        export type Direction = (typeof Direction)[keyof typeof Direction];
        /** Whether the impact was measured, estimated, or left unquantified. */
        export const Measurement = {
            Observed: "observed",
            Estimated: "estimated",
            NotMeasured: "notMeasured",
        } as const;
        export type Measurement = (typeof Measurement)[keyof typeof Measurement];
        /** How this evidence may be used in benchmark calculations. */
        export const BenchmarkUse = {
            Direct: "direct",
            Modeled: "modeled",
            AttributionOnly: "attributionOnly",
            ContextOnly: "contextOnly",
        } as const;
        export type BenchmarkUse = (typeof BenchmarkUse)[keyof typeof BenchmarkUse];
    }

    export interface Reliability {
        scoreDimension: "reliability";
        /** Reliability evidence channel informed by this item. */
        role: SessionAssessmentDimensionEffectReliability.Role;
        /** How the evidence affects this dimension. */
        direction: SessionAssessmentDimensionEffectReliability.Direction;
        /** Whether the impact was measured, estimated, or left unquantified. */
        measurement: SessionAssessmentDimensionEffectReliability.Measurement;
        /** How this evidence may be used in benchmark calculations. */
        benchmarkUse: SessionAssessmentDimensionEffectReliability.BenchmarkUse;
        impact?: Latitude.SessionAssessmentEvidenceImpact | undefined;
        costEvaluation?: Latitude.SessionAssessmentCostEvaluation | undefined;
    }

    export namespace SessionAssessmentDimensionEffectReliability {
        /** Reliability evidence channel informed by this item. */
        export const Role = {
            CompletionOutcome: "completionOutcome",
            OperationalIncident: "operationalIncident",
        } as const;
        export type Role = (typeof Role)[keyof typeof Role];
        /** How the evidence affects this dimension. */
        export const Direction = {
            Positive: "positive",
            Negative: "negative",
            Context: "context",
        } as const;
        export type Direction = (typeof Direction)[keyof typeof Direction];
        /** Whether the impact was measured, estimated, or left unquantified. */
        export const Measurement = {
            Observed: "observed",
            Estimated: "estimated",
            NotMeasured: "notMeasured",
        } as const;
        export type Measurement = (typeof Measurement)[keyof typeof Measurement];
        /** How this evidence may be used in benchmark calculations. */
        export const BenchmarkUse = {
            Direct: "direct",
            Modeled: "modeled",
            AttributionOnly: "attributionOnly",
            ContextOnly: "contextOnly",
        } as const;
        export type BenchmarkUse = (typeof BenchmarkUse)[keyof typeof BenchmarkUse];
    }

    export interface Cost {
        scoreDimension: "cost";
        /** Evidence about useful versus avoidable spend. */
        role: SessionAssessmentDimensionEffectCost.Role;
        /** How the evidence affects this dimension. */
        direction: SessionAssessmentDimensionEffectCost.Direction;
        /** Whether the impact was measured, estimated, or left unquantified. */
        measurement: SessionAssessmentDimensionEffectCost.Measurement;
        /** How this evidence may be used in benchmark calculations. */
        benchmarkUse: SessionAssessmentDimensionEffectCost.BenchmarkUse;
        impact?: Latitude.SessionAssessmentEvidenceImpact | undefined;
        costEvaluation?: Latitude.SessionAssessmentCostEvaluation | undefined;
    }

    export namespace SessionAssessmentDimensionEffectCost {
        /** Evidence about useful versus avoidable spend. */
        export const Role = {
            SpendEfficiency: "spendEfficiency",
        } as const;
        export type Role = (typeof Role)[keyof typeof Role];
        /** How the evidence affects this dimension. */
        export const Direction = {
            Positive: "positive",
            Negative: "negative",
            Context: "context",
        } as const;
        export type Direction = (typeof Direction)[keyof typeof Direction];
        /** Whether the impact was measured, estimated, or left unquantified. */
        export const Measurement = {
            Observed: "observed",
            Estimated: "estimated",
            NotMeasured: "notMeasured",
        } as const;
        export type Measurement = (typeof Measurement)[keyof typeof Measurement];
        /** How this evidence may be used in benchmark calculations. */
        export const BenchmarkUse = {
            Direct: "direct",
            Modeled: "modeled",
            AttributionOnly: "attributionOnly",
            ContextOnly: "contextOnly",
        } as const;
        export type BenchmarkUse = (typeof BenchmarkUse)[keyof typeof BenchmarkUse];
    }

    export interface Speed {
        scoreDimension: "speed";
        /** Evidence about critical-path efficiency. */
        role: SessionAssessmentDimensionEffectSpeed.Role;
        /** How the evidence affects this dimension. */
        direction: SessionAssessmentDimensionEffectSpeed.Direction;
        /** Whether the impact was measured, estimated, or left unquantified. */
        measurement: SessionAssessmentDimensionEffectSpeed.Measurement;
        /** How this evidence may be used in benchmark calculations. */
        benchmarkUse: SessionAssessmentDimensionEffectSpeed.BenchmarkUse;
        impact?: Latitude.SessionAssessmentEvidenceImpact | undefined;
        costEvaluation?: Latitude.SessionAssessmentCostEvaluation | undefined;
    }

    export namespace SessionAssessmentDimensionEffectSpeed {
        /** Evidence about critical-path efficiency. */
        export const Role = {
            CriticalPathEfficiency: "criticalPathEfficiency",
        } as const;
        export type Role = (typeof Role)[keyof typeof Role];
        /** How the evidence affects this dimension. */
        export const Direction = {
            Positive: "positive",
            Negative: "negative",
            Context: "context",
        } as const;
        export type Direction = (typeof Direction)[keyof typeof Direction];
        /** Whether the impact was measured, estimated, or left unquantified. */
        export const Measurement = {
            Observed: "observed",
            Estimated: "estimated",
            NotMeasured: "notMeasured",
        } as const;
        export type Measurement = (typeof Measurement)[keyof typeof Measurement];
        /** How this evidence may be used in benchmark calculations. */
        export const BenchmarkUse = {
            Direct: "direct",
            Modeled: "modeled",
            AttributionOnly: "attributionOnly",
            ContextOnly: "contextOnly",
        } as const;
        export type BenchmarkUse = (typeof BenchmarkUse)[keyof typeof BenchmarkUse];
    }

    export interface Safety {
        scoreDimension: "safety";
        /** Safety evidence channel informed by this item. */
        role: SessionAssessmentDimensionEffectSafety.Role;
        /** How the evidence affects this dimension. */
        direction: SessionAssessmentDimensionEffectSafety.Direction;
        /** Whether the impact was measured, estimated, or left unquantified. */
        measurement: SessionAssessmentDimensionEffectSafety.Measurement;
        /** How this evidence may be used in benchmark calculations. */
        benchmarkUse: SessionAssessmentDimensionEffectSafety.BenchmarkUse;
        impact?: Latitude.SessionAssessmentEvidenceImpact | undefined;
        costEvaluation?: Latitude.SessionAssessmentCostEvaluation | undefined;
    }

    export namespace SessionAssessmentDimensionEffectSafety {
        /** Safety evidence channel informed by this item. */
        export const Role = {
            ConfirmedHarm: "confirmedHarm",
            Exposure: "exposure",
            SuccessfulDefense: "successfulDefense",
        } as const;
        export type Role = (typeof Role)[keyof typeof Role];
        /** How the evidence affects this dimension. */
        export const Direction = {
            Positive: "positive",
            Negative: "negative",
            Context: "context",
        } as const;
        export type Direction = (typeof Direction)[keyof typeof Direction];
        /** Whether the impact was measured, estimated, or left unquantified. */
        export const Measurement = {
            Observed: "observed",
            Estimated: "estimated",
            NotMeasured: "notMeasured",
        } as const;
        export type Measurement = (typeof Measurement)[keyof typeof Measurement];
        /** How this evidence may be used in benchmark calculations. */
        export const BenchmarkUse = {
            Direct: "direct",
            Modeled: "modeled",
            AttributionOnly: "attributionOnly",
            ContextOnly: "contextOnly",
        } as const;
        export type BenchmarkUse = (typeof BenchmarkUse)[keyof typeof BenchmarkUse];
    }
}
