// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export type SessionAssessmentEvidenceAnchor =
    | Latitude.SessionAssessmentEvidenceAnchor.Message
    | Latitude.SessionAssessmentEvidenceAnchor.Span
    | Latitude.SessionAssessmentEvidenceAnchor.ToolCall
    | Latitude.SessionAssessmentEvidenceAnchor.MemoryEvent
    | Latitude.SessionAssessmentEvidenceAnchor.Score
    | Latitude.SessionAssessmentEvidenceAnchor.Signal;

export namespace SessionAssessmentEvidenceAnchor {
    export interface Message {
        kind: "message";
        /** Trace containing the referenced message. */
        traceId: string;
        /** Zero-based message position in the trace conversation. */
        messageIndex: number;
        /** Zero-based content-part position within the message. */
        partIndex?: number | undefined;
        /** Stable hash used to recognize the referenced message content. */
        contentHash?: string | undefined;
    }

    export interface Span {
        kind: "span";
        /** Trace containing the referenced span. */
        traceId: string;
        /** Identifier of the referenced span. */
        spanId: string;
    }

    export interface ToolCall {
        kind: "toolCall";
        /** Trace containing the referenced tool call. */
        traceId: string;
        /** Provider or SDK identifier of the referenced tool call. */
        toolCallId: string;
        /** Name of the invoked tool when available. */
        toolName?: string | undefined;
        /** Message position containing the tool call. */
        messageIndex?: number | undefined;
    }

    export interface MemoryEvent {
        kind: "memoryEvent";
        /** Identifier of the referenced memory event. */
        memoryEventId: string;
    }

    export interface Score {
        kind: "score";
        /** Identifier of the referenced score. */
        scoreId: string;
    }

    export interface Signal {
        kind: "signal";
        /** Identifier of the referenced signal. */
        signalId: string;
    }
}
