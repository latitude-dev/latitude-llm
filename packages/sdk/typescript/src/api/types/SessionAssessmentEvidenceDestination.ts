// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export type SessionAssessmentEvidenceDestination =
    | Latitude.SessionAssessmentEvidenceDestination.SessionMessage
    | Latitude.SessionAssessmentEvidenceDestination.Span
    | Latitude.SessionAssessmentEvidenceDestination.ToolCall
    | Latitude.SessionAssessmentEvidenceDestination.MemoryEvent
    | Latitude.SessionAssessmentEvidenceDestination.Score
    | Latitude.SessionAssessmentEvidenceDestination.Signal;

export namespace SessionAssessmentEvidenceDestination {
    export interface SessionMessage {
        kind: "sessionMessage";
        /** Trace containing the destination message. */
        traceId: string;
        /** Zero-based message position to open. */
        messageIndex: number;
        /** Zero-based content-part position to open. */
        partIndex?: number | undefined;
    }

    export interface Span {
        kind: "span";
        /** Trace containing the destination span. */
        traceId: string;
        /** Identifier of the destination span. */
        spanId: string;
    }

    export interface ToolCall {
        kind: "toolCall";
        /** Trace containing the destination tool call. */
        traceId: string;
        /** Identifier of the destination tool call. */
        toolCallId: string;
    }

    export interface MemoryEvent {
        kind: "memoryEvent";
        /** Identifier of the destination memory event. */
        memoryEventId: string;
    }

    export interface Score {
        kind: "score";
        /** Identifier of the destination score. */
        scoreId: string;
    }

    export interface Signal {
        kind: "signal";
        /** Identifier of the destination signal. */
        signalId: string;
    }
}
