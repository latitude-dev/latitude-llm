// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

/**
 * Native structured impact carried by this evidence.
 */
export type SessionAssessmentEvidenceImpact =
    | Latitude.SessionAssessmentEvidenceImpact.TaskOutcome
    | Latitude.SessionAssessmentEvidenceImpact.Completion
    | Latitude.SessionAssessmentEvidenceImpact.Incident
    | Latitude.SessionAssessmentEvidenceImpact.Spend
    | Latitude.SessionAssessmentEvidenceImpact.Duration
    | Latitude.SessionAssessmentEvidenceImpact.OutcomeAssociation
    | Latitude.SessionAssessmentEvidenceImpact.Safety
    | Latitude.SessionAssessmentEvidenceImpact.Observation;

export namespace SessionAssessmentEvidenceImpact {
    export interface TaskOutcome {
        kind: "taskOutcome";
        /** Observed task outcome. */
        verdict: SessionAssessmentEvidenceImpactTaskOutcome.Verdict;
        /** Estimated success probability when available. */
        probability?: number | undefined;
    }

    export namespace SessionAssessmentEvidenceImpactTaskOutcome {
        /** Observed task outcome. */
        export const Verdict = {
            Success: "success",
            Failure: "failure",
        } as const;
        export type Verdict = (typeof Verdict)[keyof typeof Verdict];
    }

    export interface Completion {
        kind: "completion";
        /** Whether the session delivered usable output. */
        status: SessionAssessmentEvidenceImpactCompletion.Status;
    }

    export namespace SessionAssessmentEvidenceImpactCompletion {
        /** Whether the session delivered usable output. */
        export const Status = {
            Usable: "usable",
            TerminalFailure: "terminalFailure",
        } as const;
        export type Status = (typeof Status)[keyof typeof Status];
    }

    export interface Incident {
        kind: "incident";
        /** Whether execution recovered after the incident. */
        status: SessionAssessmentEvidenceImpactIncident.Status;
        /** Whether the same operation or tool later succeeded. */
        sameSubjectRecovered?: boolean | undefined;
    }

    export namespace SessionAssessmentEvidenceImpactIncident {
        /** Whether execution recovered after the incident. */
        export const Status = {
            Recovered: "recovered",
            Unrecovered: "unrecovered",
        } as const;
        export type Status = (typeof Status)[keyof typeof Status];
    }

    export interface Spend {
        kind: "spend";
        /** Observed spend in microcents. */
        observedMicrocents: number;
        /** Spend attributed as avoidable when measured. */
        avoidableMicrocents?: number | undefined;
    }

    export interface Duration {
        kind: "duration";
        /** Observed duration in nanoseconds. */
        observedNs: number;
        /** Duration attributed as avoidable when measured. */
        avoidableNs?: number | undefined;
    }

    export interface OutcomeAssociation {
        kind: "outcomeAssociation";
        /** Modeled change in success probability. */
        probabilityChange?: number | undefined;
    }

    export interface Safety {
        kind: "safety";
        /** Observed safety result. */
        status: SessionAssessmentEvidenceImpactSafety.Status;
        /** Machine-readable safety finding category. */
        findingKind: string;
    }

    export namespace SessionAssessmentEvidenceImpactSafety {
        /** Observed safety result. */
        export const Status = {
            Exposure: "exposure",
            SuccessfulDefense: "successfulDefense",
            ConfirmedHarm: "confirmedHarm",
        } as const;
        export type Status = (typeof Status)[keyof typeof Status];
    }

    export interface Observation {
        kind: "observation";
        /** Native numeric observation when available. */
        value?: number | undefined;
        /** Unit of the native observation when available. */
        unit?: string | undefined;
    }
}
