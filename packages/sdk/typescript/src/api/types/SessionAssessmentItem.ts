// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export interface SessionAssessmentItem {
    /** Stable identifier for this assessment item. */
    id: string;
    /** Stable identity of the underlying fact across recomputation. */
    evidenceKey: string;
    /** Stable identity used to group equivalent findings and linked judgments. */
    groupKey: string;
    /** Short human-readable explanation of the evidence. */
    label: string;
    /** Additional explanation when available; never raw telemetry content. */
    description?: string | undefined;
    /** ISO-8601 timestamp for when the evidence occurred, when telemetry provides one. */
    occurredAt?: string | undefined;
    /** Source that identified the fact. */
    source: SessionAssessmentItem.Source;
    /** Whether the finding is harmful, beneficial, or requires interpretation. */
    polarity: SessionAssessmentItem.Polarity;
    /** Coarse impact level for ordering and display. */
    impactLevel: SessionAssessmentItem.ImpactLevel;
    /** Canonical metric identifier when the item has benchmark semantics. */
    metricId?: string | undefined;
    /** Signals linked to the same underlying fact. */
    signalIds: string[];
    /** Scores linked to the same underlying fact. */
    scoreIds: string[];
    /** Number of equivalent occurrences represented by this item. */
    occurrenceCount: number;
    /** Dimension-specific interpretations of this evidence. */
    effects: Latitude.SessionAssessmentDimensionEffect[];
    /** Positions where the fact occurred, represented only by identifiers. */
    anchors: Latitude.SessionAssessmentEvidenceAnchor[];
    /** Authorized product records that a caller can open for the underlying details. */
    destinations: Latitude.SessionAssessmentEvidenceDestination[];
}

export namespace SessionAssessmentItem {
    /** Source that identified the fact. */
    export const Source = {
        Metric: "metric",
        Signal: "signal",
        Flagger: "flagger",
        Score: "score",
        Moment: "moment",
    } as const;
    export type Source = (typeof Source)[keyof typeof Source];
    /** Whether the finding is harmful, beneficial, or requires interpretation. */
    export const Polarity = {
        Negative: "negative",
        Unknown: "unknown",
        Positive: "positive",
    } as const;
    export type Polarity = (typeof Polarity)[keyof typeof Polarity];
    /** Coarse impact level for ordering and display. */
    export const ImpactLevel = {
        Low: "low",
        Medium: "medium",
        High: "high",
    } as const;
    export type ImpactLevel = (typeof ImpactLevel)[keyof typeof ImpactLevel];
}
