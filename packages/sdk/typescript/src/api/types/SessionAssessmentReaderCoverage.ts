// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export type SessionAssessmentReaderCoverage =
    | Latitude.SessionAssessmentReaderCoverage.Examined
    | Latitude.SessionAssessmentReaderCoverage.PartiallyExamined
    | Latitude.SessionAssessmentReaderCoverage.NotExamined
    | Latitude.SessionAssessmentReaderCoverage.NotApplicable;

export namespace SessionAssessmentReaderCoverage {
    export interface Examined {
        status: "examined";
        /** Stable identifier of the evidence reader. */
        readerId: string;
        /** Human-readable reader name. */
        label: string;
        /** Dimensions the reader can inform. */
        scoreDimensions: SessionAssessmentReaderCoverageExamined.ScoreDimensions.Item[];
        /** Number of findings produced by the reader. */
        findingCount: number;
        /** Sampling details when this reader is not deterministic. */
        selection?: SessionAssessmentReaderCoverageExamined.Selection | undefined;
    }

    export namespace SessionAssessmentReaderCoverageExamined {
        export type ScoreDimensions = ScoreDimensions.Item[];

        export namespace ScoreDimensions {
            /** Agent Score dimension informed by this evidence. */
            export const Item = {
                Outcome: "outcome",
                Reliability: "reliability",
                Cost: "cost",
                Speed: "speed",
                Safety: "safety",
            } as const;
            export type Item = (typeof Item)[keyof typeof Item];
        }

        /**
         * Sampling details when this reader is not deterministic.
         */
        export interface Selection {
            /** Selection method. */
            method: Selection.Method;
            /** Probability that this session was selected. */
            inclusionProbability: number;
        }

        export namespace Selection {
            /** Selection method. */
            export const Method = {
                Deterministic: "deterministic",
                Hinted: "hinted",
                UniformSample: "uniform-sample",
                OrdinarySample: "ordinary-sample",
            } as const;
            export type Method = (typeof Method)[keyof typeof Method];
        }
    }

    export interface PartiallyExamined {
        status: "partiallyExamined";
        /** Stable identifier of the evidence reader. */
        readerId: string;
        /** Human-readable reader name. */
        label: string;
        /** Dimensions the reader can inform. */
        scoreDimensions: SessionAssessmentReaderCoveragePartiallyExamined.ScoreDimensions.Item[];
        /** Number of findings produced by readable inputs. */
        findingCount: number;
        /** Number of inputs the reader could examine. */
        readableCount: number;
        /** Total candidate inputs for the reader. */
        totalCount: number;
        /** Reason the reader could not completely examine the session. */
        limitation: SessionAssessmentReaderCoveragePartiallyExamined.Limitation;
        /** Sampling details when this reader is not deterministic. */
        selection?: SessionAssessmentReaderCoveragePartiallyExamined.Selection | undefined;
    }

    export namespace SessionAssessmentReaderCoveragePartiallyExamined {
        export type ScoreDimensions = ScoreDimensions.Item[];

        export namespace ScoreDimensions {
            /** Agent Score dimension informed by this evidence. */
            export const Item = {
                Outcome: "outcome",
                Reliability: "reliability",
                Cost: "cost",
                Speed: "speed",
                Safety: "safety",
            } as const;
            export type Item = (typeof Item)[keyof typeof Item];
        }

        /** Reason the reader could not completely examine the session. */
        export const Limitation = {
            Skipped: "skipped",
            NotSelected: "notSelected",
            RateLimited: "rateLimited",
            ExecutionFailed: "executionFailed",
            Pending: "pending",
            MissingTelemetry: "missingTelemetry",
            UnmappedTelemetry: "unmappedTelemetry",
            MissingPricing: "missingPricing",
            MissingContent: "missingContent",
            TruncatedContent: "truncatedContent",
            UnknownModelContext: "unknownModelContext",
            CriticalPathUnavailable: "criticalPathUnavailable",
        } as const;
        export type Limitation = (typeof Limitation)[keyof typeof Limitation];

        /**
         * Sampling details when this reader is not deterministic.
         */
        export interface Selection {
            /** Selection method. */
            method: Selection.Method;
            /** Probability that this session was selected. */
            inclusionProbability: number;
        }

        export namespace Selection {
            /** Selection method. */
            export const Method = {
                Deterministic: "deterministic",
                Hinted: "hinted",
                UniformSample: "uniform-sample",
                OrdinarySample: "ordinary-sample",
            } as const;
            export type Method = (typeof Method)[keyof typeof Method];
        }
    }

    export interface NotExamined {
        status: "notExamined";
        /** Stable identifier of the evidence reader. */
        readerId: string;
        /** Human-readable reader name. */
        label: string;
        /** Dimensions the reader can inform. */
        scoreDimensions: SessionAssessmentReaderCoverageNotExamined.ScoreDimensions.Item[];
        /** Reason the reader could not completely examine the session. */
        limitation: SessionAssessmentReaderCoverageNotExamined.Limitation;
        /** Sampling details when this reader is not deterministic. */
        selection?: SessionAssessmentReaderCoverageNotExamined.Selection | undefined;
    }

    export namespace SessionAssessmentReaderCoverageNotExamined {
        export type ScoreDimensions = ScoreDimensions.Item[];

        export namespace ScoreDimensions {
            /** Agent Score dimension informed by this evidence. */
            export const Item = {
                Outcome: "outcome",
                Reliability: "reliability",
                Cost: "cost",
                Speed: "speed",
                Safety: "safety",
            } as const;
            export type Item = (typeof Item)[keyof typeof Item];
        }

        /** Reason the reader could not completely examine the session. */
        export const Limitation = {
            Skipped: "skipped",
            NotSelected: "notSelected",
            RateLimited: "rateLimited",
            ExecutionFailed: "executionFailed",
            Pending: "pending",
            MissingTelemetry: "missingTelemetry",
            UnmappedTelemetry: "unmappedTelemetry",
            MissingPricing: "missingPricing",
            MissingContent: "missingContent",
            TruncatedContent: "truncatedContent",
            UnknownModelContext: "unknownModelContext",
            CriticalPathUnavailable: "criticalPathUnavailable",
        } as const;
        export type Limitation = (typeof Limitation)[keyof typeof Limitation];

        /**
         * Sampling details when this reader is not deterministic.
         */
        export interface Selection {
            /** Selection method. */
            method: Selection.Method;
            /** Probability that this session was selected. */
            inclusionProbability: number;
        }

        export namespace Selection {
            /** Selection method. */
            export const Method = {
                Deterministic: "deterministic",
                Hinted: "hinted",
                UniformSample: "uniform-sample",
                OrdinarySample: "ordinary-sample",
            } as const;
            export type Method = (typeof Method)[keyof typeof Method];
        }
    }

    export interface NotApplicable {
        status: "notApplicable";
        /** Stable identifier of the evidence reader. */
        readerId: string;
        /** Human-readable reader name. */
        label: string;
        /** Dimensions the reader can inform. */
        scoreDimensions: SessionAssessmentReaderCoverageNotApplicable.ScoreDimensions.Item[];
    }

    export namespace SessionAssessmentReaderCoverageNotApplicable {
        export type ScoreDimensions = ScoreDimensions.Item[];

        export namespace ScoreDimensions {
            /** Agent Score dimension informed by this evidence. */
            export const Item = {
                Outcome: "outcome",
                Reliability: "reliability",
                Cost: "cost",
                Speed: "speed",
                Safety: "safety",
            } as const;
            export type Item = (typeof Item)[keyof typeof Item];
        }
    }
}
