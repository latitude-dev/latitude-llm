// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export interface SessionMemorySummary {
    /** Per-record read/write token footprint for the session (or a single trace). */
    records: Latitude.MemoryRecordSummary[];
    /** Session-wide totals. */
    total: SessionMemorySummary.Total;
}

export namespace SessionMemorySummary {
    /**
     * Session-wide totals.
     */
    export interface Total {
        /** Total tokens read across the session. */
        readTokens: number;
        /** Total tokens added across the session. */
        tokensAdded: number;
        /** Total tokens removed across the session. */
        tokensRemoved: number;
        /** Number of records the session wrote, including zero-delta writes. */
        writeRecords: number;
    }
}
