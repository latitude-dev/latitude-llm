// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

/**
 * Whether the signal is currently being monitored: `automatic`, `idle`, `generating`, or `realigning`.
 */
export type SignalMonitoringState =
    /**
     * The signal is automatically monitored by the system and does not need an evaluation. */
    | Latitude.SignalMonitoringState.Automatic
    /**
     * The signal is not currently being monitored. */
    | Latitude.SignalMonitoringState.Idle
    /**
     * An evaluation is being generated for this signal. The signal has no active evaluation yet. */
    | Latitude.SignalMonitoringState.Generating
    /**
     * An active evaluation is being realigned. */
    | Latitude.SignalMonitoringState.Realigning;

export namespace SignalMonitoringState {
    export interface Automatic {
        kind: "automatic";
    }

    export interface Idle {
        kind: "idle";
    }

    export interface Generating {
        kind: "generating";
    }

    export interface Realigning {
        kind: "realigning";
        /** Id of the evaluation currently being realigned. */
        evaluationId: string;
    }
}
