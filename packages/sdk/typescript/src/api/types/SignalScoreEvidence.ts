// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export type SignalScoreEvidence =
    | Latitude.SignalScoreEvidence.Outcome
    | Latitude.SignalScoreEvidence.Reliability
    | Latitude.SignalScoreEvidence.Cost
    | Latitude.SignalScoreEvidence.Speed
    | Latitude.SignalScoreEvidence.Safety;

export namespace SignalScoreEvidence {
    export interface Outcome {
        scoreDimension: "outcome";
        /** How this evidence informs the dimension. */
        role: SignalScoreEvidenceOutcome.Role;
    }

    export namespace SignalScoreEvidenceOutcome {
        /** How this evidence informs the dimension. */
        export const Role = {
            TaskOutcome: "taskOutcome",
        } as const;
        export type Role = (typeof Role)[keyof typeof Role];
    }

    export interface Reliability {
        scoreDimension: "reliability";
        /** How this evidence informs the dimension. */
        role: SignalScoreEvidenceReliability.Role;
    }

    export namespace SignalScoreEvidenceReliability {
        /** How this evidence informs the dimension. */
        export const Role = {
            CompletionOutcome: "completionOutcome",
            OperationalIncident: "operationalIncident",
        } as const;
        export type Role = (typeof Role)[keyof typeof Role];
    }

    export interface Cost {
        scoreDimension: "cost";
        /** How this evidence informs the dimension. */
        role: SignalScoreEvidenceCost.Role;
    }

    export namespace SignalScoreEvidenceCost {
        /** How this evidence informs the dimension. */
        export const Role = {
            SpendEfficiency: "spendEfficiency",
        } as const;
        export type Role = (typeof Role)[keyof typeof Role];
    }

    export interface Speed {
        scoreDimension: "speed";
        /** How this evidence informs the dimension. */
        role: SignalScoreEvidenceSpeed.Role;
    }

    export namespace SignalScoreEvidenceSpeed {
        /** How this evidence informs the dimension. */
        export const Role = {
            CriticalPathEfficiency: "criticalPathEfficiency",
        } as const;
        export type Role = (typeof Role)[keyof typeof Role];
    }

    export interface Safety {
        scoreDimension: "safety";
        /** How this evidence informs the dimension. */
        role: SignalScoreEvidenceSafety.Role;
    }

    export namespace SignalScoreEvidenceSafety {
        /** How this evidence informs the dimension. */
        export const Role = {
            ConfirmedHarm: "confirmedHarm",
            Exposure: "exposure",
        } as const;
        export type Role = (typeof Role)[keyof typeof Role];
    }
}
