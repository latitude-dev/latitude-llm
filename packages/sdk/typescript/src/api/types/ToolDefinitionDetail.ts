// This file was auto-generated by Fern from our API Definition.

/**
 * Latest definition seen for the tool. `null` when never offered in the range.
 */
export interface ToolDefinitionDetail {
    /** Parsed name and description. `null` when the stored definition could not be parsed. */
    definition: ToolDefinitionDetail.Definition | null;
    /** Raw tool definition JSON, verbatim, including its `parameters`. */
    definitionJson: string;
    /** LLM turns that offered this tool in the range. */
    offeredCount: number;
    /** Distinct traces that offered this tool. */
    offeredTraces: number;
    /** ISO-8601 timestamp the tool was last offered. */
    lastOffered: string;
}

export namespace ToolDefinitionDetail {
    /**
     * Parsed name and description. `null` when the stored definition could not be parsed.
     */
    export interface Definition {
        /** Tool name from the definition. */
        name: string;
        /** Tool description from the definition. */
        description: string;
    }
}
