// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

/**
 * Which traces to include in the export — either explicit ids or a filter set.
 */
export type TracesRef = Latitude.TracesRef.Ids | Latitude.TracesRef.Filters;

export namespace TracesRef {
    export interface Ids {
        by: "ids";
        /** Non-empty list of trace identifiers. */
        ids: string[];
    }

    export interface Filters {
        by: "filters";
        filters: Latitude.FilterSet;
    }
}
