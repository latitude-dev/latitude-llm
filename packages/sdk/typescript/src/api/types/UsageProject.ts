// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export interface UsageProject {
    /** Stable project identifier. */
    id: string;
    /** Project slug. `null` when the project no longer exists. */
    slug: string | null;
    /** Project name. `null` when the project no longer exists. */
    name: string | null;
    /** Credits the project spent during the period. */
    credits: number;
    /** The project's credits by product area, largest first. */
    categories: Latitude.UsageCategory[];
}
