// This file was auto-generated by Fern from our API Definition.

import type * as Latitude from "../index.js";

export interface UsageResponse {
    /** Billing plan the organization is on. */
    plan: UsageResponse.Plan;
    /** Billing period the usage figures cover. */
    period: UsageResponse.Period;
    /** Credit position for the period. */
    credits: UsageResponse.Credits;
    /** Credits by product area across all projects, largest first. */
    categories: Latitude.UsageCategory[];
    /** Credits by project, largest first. */
    projects: Latitude.UsageProject[];
}

export namespace UsageResponse {
    /** Billing plan the organization is on. */
    export const Plan = {
        Free: "free",
        Pro: "pro",
        Enterprise: "enterprise",
        SelfHosted: "self-hosted",
    } as const;
    export type Plan = (typeof Plan)[keyof typeof Plan];

    /**
     * Billing period the usage figures cover.
     */
    export interface Period {
        /** ISO-8601 start of the current billing period. */
        start: string;
        /** ISO-8601 end of the current billing period, exclusive. */
        end: string;
    }

    /**
     * Credit position for the period.
     */
    export interface Credits {
        /** Credits included in the plan for the period. `null` when the plan is unlimited. */
        included: number | null;
        /** Credits used so far this period, overage included. */
        consumed: number;
        /** Included credits still available. `null` when the plan is unlimited. */
        remaining: number | null;
        /** Credits used beyond the included allowance. */
        overage: number;
        /** Whether usage can continue once the included credits are spent. */
        overageAllowed: boolean;
    }
}
