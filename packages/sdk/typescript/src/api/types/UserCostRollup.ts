// This file was auto-generated by Fern from our API Definition.

/**
 * Cost aggregates across every matching user, not just this page.
 */
export interface UserCostRollup {
    /** Sum of every matching user's total cost, in microcents. */
    sum: number;
    /** Mean of every matching user's mean per-trace cost, in microcents. */
    avg: number;
    /** Median of every matching user's median per-trace cost, in microcents. */
    median: number;
}
